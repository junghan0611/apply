# NOW — NHN 제출 레인: 이미지 6장 생성

- **Stem**: NHN 제출본 3종을 "이 사람이 이걸 냈다"가 성립하는 완성도까지 올린다. 담금질 루프 —
  한 문서 품질을 올리고 그 기준으로 다른 문서를 되돌려 고친다. 몇 바퀴 더 돈다.
- **Current**: **글·증거·프롬프트는 닫혔다.** 3종 org 모두 v3, timeline 접합 완료,
  이미지 프롬프트 6종 재작성 완료. 남은 것은 이미지뿐이다.
  *현재 `images/` 6장은 폐기된 v2 흰 배경 벡터본이다* — 본문은 GLGMAN 세계관을 전제한다.
- **Next**: (1) **이미지 6장 생성** ← 다음 한 걸음. `glg-image` 스킬로, `competency.org`의
  공통 세계관 블록 + 각 장면 프롬프트를 이어 붙여 생성 → (2) 아래 기준으로 육안 검수 →
  (3) 채택 후 `./run.sh images` → (4) `./run.sh all && ./run.sh verify` → (5) 쪽수·배치 검토.
- **이미지 검수 기준**: ① 글자가 하나라도 보이면 재생성 ② 캐릭터 수(⑤ 여우 정확히 넷, 그중
  하나는 담 밖 / ① 문은 정확히 셋) ③ 경계가 실제로 끊겨 있는지(② 붉은 경로, ④ 암벽)
  ④ 상태 구분(밝음=운영 중 / 희미함=로드맵) ⑤ 5장이 한 연작으로 보이는지.
- **Blocker**: none.
- **Read**: 루트 `AGENTS.md` → `nhn/AGENTS.md` → `nhn/PORTFOLIO-PLAN.md`(v3) →
  `nhn/portfolio.org` 하단 `:noexport:` v3 절.
- **참고**: 2026-07-20 세션 끝에 구이미지 상태로 PDF를 한 번 빌드했다(쪽수·조판 확인용).
  그 산출물은 제출본이 아니다. 이미지 채택 후 반드시 다시 빌드한다.

## 회수된 앵커 — 본문 주입만 남음 (재수집 불필요)

**P1** — 1단계 arc가 timeline에 보인다: 2025-06 클러스터 스펙 → 07-02 10GbE 네트워크 →
07-03 GPU 클러스터 관리·스토리지 → 07-19 n8n·도커 워크플로 → 07-24 NixOS AI 클러스터 선언적 배포
→ 07-25/26 헬프데스크 자동화 연동. 2단계: **2026-05-07 = voscli 착수일** — 그날 하루에 스캐폴드 →
데이터 가드 정책 → EDN↔JSONB 무손실 계약 → convert/validate → README까지. *«도구를 만들기 전에
계약을 먼저 세웠다»가 커밋 순서로 증명된다.* 같은 날 기록에 "abductcli 회사판"이라는 표현이 있어
«사적 실험이 다른 몸으로 회사 워크벤치가 됐다»는 본문 주장이 검증됨. 2026-02-19에 회사 운영
인시던트 대응(Erlang VM soft lockup, zram swap) 기록도 있음.

**P4** — pi-shell-acp → entwurf 전환 시점이 **2026-03-20~25**로 특정된다(하이데거 삼부작
geworfen·andenken·Entwurf 리네이밍 → 03-25 ENTWURF.md 가이드 분리). **2026-05-27 = forge-config
착수일** — 그날 하루에 scaffold → bin/forge 4-command → 결정 6/6 → work forge 가동 검증 →
multi-profile → issue-create verb. 멱등성 문제의식은 2026-03-05 기록에 이미 있음(P0로 표기).

**P5** — `sks-hub-zig` 로컬 994 커밋, 2025-12-07~2026-07-15, 작업일 90일.
월별 260(12월)·312(1월)·103(2월)·**198(3월)**. **`198커밋/30일`은 보수적 수치다 — 최다 월은 312.**
`homeagent-config` 387 커밋 중 **2026-03-16이 정확히 24 커밋** = «3-에이전트 병렬 하루» 검증됨
(최다일은 02-23의 34). 그리고 **2025-09-29/30 «게이트웨이 프로젝트 긴급 인수인계»** 기록 —
*넘겨받아서 양산까지 보냈다*는 서사가 P5에 없다. 넣을 가치가 크다.

⚠ **timeline 원시 데이터에 사람 실명이 섞여 있다. 어떤 산출물에도 넣지 않는다.**
⚠ **`198커밋/30일`을 임의로 올리지 말 것.** GLG의 공개 이력서 수치다. 바꿀지는 GLG가 정한다.
- **Blocker**: none.
- **Read**: 루트 `AGENTS.md` → `nhn/AGENTS.md` → `nhn/portfolio.org` 의 `:noexport:` v3 절
  → `nhn/PORTFOLIO-PLAN.md`.
- **PM**: GPT 세션 garden id `20260720T055951-8c3c4b` (socket-citizen). 구조·분량·순서 판단은
  이쪽. **competency 첫 판이 나오면 체크포인트로 보낼 것** (PM 요청).

## competency.org 수선 지침 — 다음 세션의 본 작업

PM 지시: *본문을 반복하지 말 것.* NHN 판독자가 3~5분 안에
`구축(P1) / 운영(P2) / RAG(P3) / Agentic workflow(P4) / BE·제품(P5) / 문서·전파(인계 절)` 를
찾을 수 있는 **압축 지도**여야 한다. portfolio가 증명이고 competency는 색인이다.

**단 압축은 삭제가 아니다.** 이번 라운드 목표는 문장 축소가 아니라 *위 여섯 칸이 하나도 비어
있지 않게 하는 것* 이다. competency를 고치다가 portfolio에 빠진 실적을 발견하면 먼저 소재 원장에
올리고 양쪽 역할에 맞게 배치한다 — 발견을 버리지 않는다.

- 라벨 3종으로 통일 (`[사내 · 설계 공개]` 같은 겹친 라벨 제거).
- 프롤로그가 portfolio의 새 논지와 정합한지 — 지금은 "PKM → 조직 작업면" 성장 서사다.
- 누락 소재 점검: AX 1단계 기반 · PSIRT · HomeAgent · 작업량 수치 · **P2→P3 기억 계보**.
- 막판 글다듬기로 미룬 것: P1 첫 문장을 부정형("기반이 없었다")에서 긍정형("첫 단계로 공용 GPU·
  임베딩·데이터 파이프라인 기반을 구축했다")으로. 지금 이 한 문장으로 진행을 막지 않는다.

## 이번 라운드에 확정한 규칙 — 다음 세션이 반드시 지킬 것

- **수치: 성능 ✗ / 작업량·수용 ✓.** 부팅 45.4→5.8초, 메모리 816→246MiB 류는 계속 뺀다
  (벤치마크 자랑이고 검증 불가). 198커밋/30일, 하루 24커밋·테스트 163·충돌 0, 코드공유 96%,
  npm 1,395, PR +885줄은 **넣는다** — "에이전트와 일한다"는 주장의 직접 증거다. 이 구분을 잃으면
  지난 라운드의 "수치 빼자" 결정과 충돌하는 것처럼 보인다.
- **라벨 3종은 등급이 아니라 확인 경로다.** `[공개]` 지금 링크로 확인 가능 / `[사내]` 외부에서
  열 수 없음 / `[운영 기록]` timeline으로 날짜·사건 제시 가능. **서열도 배타 분류도 아니라서
  `[사내]`+`[운영 기록]` 조합이 정상이다.** `[사내·비공개]`·`[비공개]`·`[사내 · 설계 공개]` 는
  되살리지 않는다.
- **소재 정본은 두 층이다.** ① 공개 베이스 3종(`~/repos/gh/junghan0611/README.md` ·
  `resume/README.md` · `apply/portfolio.md`) = **서사와 누락을 찾는 베이스**. ② `MATERIAL.md` +
  실제 repo·timeline = **사실 교정과 증거의 SSOT**. *어느 한쪽을 이유로 다른 쪽의 풍부한 소재를
  줄이지 않는다.* 제출본이 베이스보다 얕으면 문장을 다듬을 게 아니라 소재를 회수한다.
- **막힌 사실은 없다고 가정하고 세션과 각 리포에서 먼저 찾는다.** andenken 세션 기억은
  2026-07-20 최신화됐다. 문장이 얕거나 증거가 비어 보이면 `session_search`를 추상 질의→검색 결과의
  파일명·프로젝트명·명령·고유어로 재검색하는 2-pass로 쓰고, 해당 리포의 AGENTS/NEXT/README/docs와
  실제 코드·커밋으로 확인한다. *답은 각 작업 리포와 세션에 이미 축적돼 있음을 신뢰한다.* 찾기 전에
  일반화하거나 자기검열로 줄이지 말고, 그래도 경계가 불명확할 때만 GLG에게 묻는다.
- **페이지 예산은 없다.** 마스터 Org와 상세 문서는 필요한 만큼 길어도 된다. 들어갈 내용을 전부
  회수하고 증거를 붙이는 것이 먼저다. 나중에 PDF를 보고 짧은 제출 컷을 *파생* 할 수는 있어도
  **마스터에서 소재를 삭제하지 않는다.**
- **주장은 줄여서가 아니라 경계를 선명히 해서 강해진다.** 소재 회수 단계에서는 강한 원문을
  보존하고, 최종 기술 교열에서 범위만 정밀화한다. "반격당하지 않으려고 줄인다"가 아니라
  "실제 구현 경계를 더 정확히 써서 주장을 단단하게 한다"가 원칙이다.
- **자기검열로 글을 줄이지 않는다.** GLG: 수치가 좀 틀려도 되고, 사내 코드를 못 열면
  timeline 수준의 작업량으로 증명한다.

## 이미지 — 프롬프트만 담금질, 생성은 다음 세션

- 6장 전부 재생성 예정. 지금은 `:noexport:` 안의 프롬프트만 다듬는다.
- **이미지 안에 한글·문장을 넣지 않는다. 고유명사 영문 최소 토큰만.** 제목·라벨·범례는
  Org 캡션과 본문이 진다.
- 근거: `entwurf/docs/assets/entwurf-hero.jpg` 를 확대하면 **`PACP·ACPF`** 라는 존재하지 않는
  문자열이 박혀 있다. 영문 4글자가 그러면 한글은 더하다.
- **`fig-lineage` 현재 프롬프트는 폐기 대상** — `ACP: Claude Code · Codex 위임` 이 v3에서
  바로잡은 사실과 충돌한다. 파일 안에 ⚠ 표시해 뒀다.
- 세계관: GLGMAN 공통 블록 v2 = `~/sync/org/botlog/20260327T100239--힣맨-세계관-비주얼-컨셉…org`.
  생성은 `glg-image` 스킬(기본 flash 모델).

## durable fact — 문서에 반드시 들어갈 계보

- **andenken은 OpenClaw의 semantic-memory 로직을 가져와 GLG 방식으로 분리·확장한 것이다.**
  특정 런타임 내부의 기억에서 출발해 로컬 세션과 공개 가든까지 검색면을 넓히고, vector+BM25 ·
  회수 추적 · dictcli 개인 어휘 그래프를 붙였다. 즉 **P2→P3는 병렬 프로젝트가 아니라
  `OpenClaw 운영에서 얻은 기억 로직 → 런타임 밖의 가든 기억 계층` 이라는 계보다.**
  근거: GLG 원문 SSOT (`nhn/competency.org:146-147`) — "openclaw가 나오고… 액티브 메모리·드리밍
  등 기억축에 대한 고민이 화두가 되었지. 그 대안으로… 별개의 프로젝트를 만들게 된 거야",
  "openclaw와 여러 오픈소스 프로젝트들에서 필요한 부분을 내가 원하는 방향에서 가져온다."
  **competency에도 이 본질이 들어가야 한다.** (portfolio P3에는 아직 미반영 — 재접합 때 넣는다.)
- **Codex 경계는 «현재 상태»로만 쓴다.** 제출 시점 사실: Codex는 managed citizen도 reply rail도
  아닌 외부 MCP 송신자이고, ACP 백엔드는 현재 Claude 중심이다. *방향*: Codex는 과거 ACP 지원을
  거쳤고 entwurf 전환 뒤 약 0.14.0 레인에서 다시 지원할 계획이다. GPT는 pi에 네이티브로 이미 잘
  붙어 ACP 우선순위가 낮다. **본문에는 구현된 현재 상태만 주장하되, 이것을 영구적 제품 철학으로
  오해하지 않게 한다.**

## PM이 유지한 결정 — 다시 논의하지 말 것

1. **프로젝트 순서 1→2→3→4→5 유지.** P5를 앞으로 올리면 임베디드 지원서로 읽힌다. 끝에 두어
   capstone으로 쓴다.
2. **P2는 2번 유지.** P1=조직 기반 구축·도메인 착지 / P2=상류 변경·장애를 운영 규칙으로 번역.
   기능이 겹쳐도 역할이 다르고, P2가 andenken→entwurf 계보의 기점이다(위 durable fact).
3. **파이프라인 비개입.**

## timeline 회수 — competency 다음 한 걸음

**모든 날짜·커밋·사건을 먼저 전부 회수한다. PDF 선별은 막판에 한다.** (이전의 "프로젝트당 앵커
최대 3개"는 폐기됐다 — 상세 MD에는 전부 남길 수 있다.)

착수 순서 P2→P1→P4→P5. P2는 2026-04-28 롤백 / 5.2 승격일이 이미 회수돼 있어 가장 빨리
단단해진다. P1은 "운영팀 첫 실사용·피드백이 도구·문서·이슈로 돌아온 사건". P4는 공개 이슈
앵커로 `[운영 기록]`→`[공개]` 승격. P5는 출하 이정표.
`collect.py` 아직 안 돌았다 — `as_of` 고정하고 시작할 것.

## Do not touch

- `run.sh` / `flake.nix` / `pipeline/` — 다른 세션이 verify GREEN·패키지 4.8MB로 닫아 뒀다.
  근거 없이 다시 열지 않는다. 이미지 경량화 계약(256색 → 회화체에서 밴딩 실측 확인)은
  **생성 결과를 보고** 정한다. GLG: "퀄리티 올리고 나중에 사이즈 줄이면 된다."
- 공개 `junghan0611/apply/ax/` · 파생 ODT/PDF/MD 수기 수정 · 추천인·지원 경로·근무지
  · 원시 고객 식별자·자격증명 · 고객사/유통사 실명(회사는 "스마트홈 IoT 회사"로 일반화 유지).

# RECENT

- [2026-07-20] **이미지 프롬프트 6종 전면 재작성 (GLGMAN 세계관).** 공통 세계관 블록은
  `competency.org`에 한 번만 두고 6장이 공유(화풍 분산 방지). **이미지 안 문자 0 — 고유명사조차
  넣지 않았다**(hero 이미지 `PACP·ACPF` 전례). 판형 ①16:9 hero / ②~⑥3:2. 각 장면이 옮긴 판단:
  ①좁은 검토 아치를 지나야만 원본으로 회수 ②손대지 않은 저수조+끊긴 붉은 경로 ③레버 든 손과
  원장에 적는 손이 같은 순간 ④두 동굴 사이 안 뚫린 암벽+3단 회수 ⑤문 없는 담 밖의 네 번째 여우
  (=Codex 비시민) ⑥낡은 아래 두 단과 새 위 두 단의 이음매+빈 작업대. ②④는 캐릭터 없음.
  구 v2 프롬프트는 **삭제**했다(폐기 표시만 두면 다음 세션이 잘못 생성한다).
  `PORTFOLIO-PLAN.md`를 v3로 개정 — 이미지 계약·수치 원칙·페이지 예산·진행 순서 충돌 정리.
- [2026-07-20] **timeline 전체 회수 + P1·P4·P5 주입.** P2 표에 공개 커밋 링크 3건을 달아
  `[운영 기록]`→`[공개]` 승격. `detail.org`에 **시간축 증거 원장** 신설(관측 지문·소스 상태·
  선별 사건 9건·커버리지 공백). 새 사실: P5 «2025-09-29 긴급 인수인계» → 절 성격이 «처음부터
  설계»에서 «넘겨받아 완성»으로 바뀜. P4는 «이름 변경»이 아니라 «어댑터 → garden-citizen 기층»
  문제 정의 전환으로. P1은 2026-05-07 첫날 커밋 순서가 «기능보다 계약 먼저»의 증거.
- [2026-07-20] **competency 첫 판.** 비어 있던 세 칸을 채웠다 — ① 구축(본문에 GPU·n8n·Airbyte·
  Zero Trust가 **한 번도 없었다**. 2단계 arc로 재작성) ③ RAG(3층 회수 + dictcli) ⑤ BE·제품(양산
  출하·프로토콜1/백엔드2·Matter 96%·198커밋/24커밋·충돌0). **P2→P3 계보를 OpenClaw 문단 바로 뒤에
  "그 운영에서 다음 계층이 나왔습니다"로 배치** — 병렬이 아니라 인과로 읽히게. 계보는 andenken
  `AGENTS.md:35`(memory-host-sdk → LanceDB 포팅)로 교차확인. 문서·전파 칸에 **`voscli/docs/
  onboarding.md` 7항목 인수 가이드** 추가(실물 10.9KB 확인) — JD 주요업무②의 직접 증거이고
  memex-kb보다 강하다(도구가 아니라 실제 인계 행위). 라벨 `[사내 · 설계 공개]`→`[사내]` 통일.
  *넣지 않은 것*: incidentcli 9 collectors·DMS 2,749-table·StarRocks, voscli 330일 backfill,
  `reranker-server`·`notion-embedding` 프로젝트명 — competency는 색인이라 detail.org 자리로 본다.
- [2026-07-20] **PM 지적 4점 반영.** (A) P4 Codex를 시민 표에서 분리 — 외부 MCP 송신자일 뿐
  주소 가능한 시민도 회신 레일도 아니다(제 표가 틀렸다). 한 줄 명제도 "각자의 런타임을 지킨 채
  같은 garden id 표면에서 협업"으로 정밀화. (B) P3 절대명제 완화. (C) P1 "기반이 아예 없었다"를
  "공용 GPU·임베딩·파이프라인이 없었다"로 좁힘. (D) 라벨을 등급이 아니라 확인 경로로 재정의.
- [2026-07-20] portfolio v3 소재 회수: P1 전면 재작성(**AX 1단계 기반이 통째로 누락돼 있었다** —
  GPU 클러스터·n8n 40여 노드·pgvector 2,945건·Airbyte·Zero Trust. JD 주요업무① 정중앙인데
  "보조 인프라" 한 줄이었음). P5 프레임 전환(방어적 배경 → 에이전트와 함께 출하한 작업량).
  P4 사실 교정. P3 3층 크로스링궐 검색 회수. P2 4봇 exoself·botlog. 인계 절 강화.
  신설 "이 포트폴리오를 검증하는 법"(제3자 행동 표).
- [2026-07-20] 교열 정정: 이전 세션의 "P1은 공개 증거가 없으니 짧게 끝내라" 권고는 **틀렸다.**
  `abductcli` 가 P1의 공개 증거 — 사적 실험이 다른 몸으로 회사 워크벤치가 됐다는 게 P1의 제일 강한 줄.
- [2026-07-19~20] 파이프라인 세션이 LaTeX 트랙·verify 확장(A4·Missing character·ODT 활자 계약·
  이미지 임베드·DOC)·패키지를 GREEN으로 닫음. `images/master` ↔ `images` 파생 분리 완료.
- [2026-07-18] competency 5p GREEN, portfolio v2 10p. 인포그래픽 5장 2K 교체.

# LEDGER

- 소재 SSOT: 공개 베이스 3종(위 규칙 절) + `~/repos/gh/junghan0611/apply/MATERIAL.md`.
- 포트폴리오 결정·이미지 계약: `nhn/PORTFOLIO-PLAN.md`. **단 이미지 계약(흰 배경 벡터)은
  GLGMAN 세계관으로 전환 예정 — 프롬프트 확정 시 이 문서를 먼저 개정한다.**
- 제출물 SSOT: Org·이미지·파이프라인. 파생물은 재빌드한다.
- 최종 강조 순서·회사명 노출·커밋·푸시는 GLG가 결정한다.
