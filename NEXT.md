# RAIL — 현재 좌표

- [x] **1. 공개 안전 계약·history 정제·새 object database 검증**
- [x] **2. 기존 지원 기록 회수와 리멤버·원티드 사후 회수**
- [x] **3. 시스템 축 판정 기준 수립과 전 후보 재분류** — `AGENTS.md` §시스템 축 (2026-09-18 GLG)
- [x] **4. 시스템 축 22건 케이스 생성 + 요건 실측** — 전부 `draft`, `check.py` ok 80건
- [x] **5. 이력서 재빌드 완료(7종)** — `platform-engineer` 신설·FDE 컷 축 정정·verify 통과.
- [x] **6. 폼 실측 17건** — 원티드 15 + 그리팅 1 + 리멤버 1. 컷도 17건 전부 확정
- [ ] **7. 컷 PDF 를 원티드 [이력서] 탭에 올린다 (GLG)** ← CURRENT. 이것만이 제출을 막는다
- [ ] **8. GLG 브라우저 제출 → 원장·지문 닫기**

현재 좌표: 1~6 완료 → **7 이력서 탭 업로드(GLG 몫)** → 8 제출

✅ **공개 게이트 통과.** 2026-09-18 에 실패했던 넷은 셋이 오탐(ATS 발신 주소·noreply·Lever
공개 공고 posting id)이고 하나가 정책 허용(GLG 가 공개하기로 결정한 커밋 ident)이었다.
`scripts/check-public.sh` 의 `neutralize()` 를 좁히고 self-test 를 붙여 닫았다.

# NOW

## 🔴 CURRENT — 컷 PDF 를 원티드 [이력서] 탭에 올린다. 이것만 남았다

폼 실측은 **끝났다**(2026-09-18). 병목의 정체가 바뀌었다 — 「폼 문항을 몰라서」가 아니라
**「컷 PDF 가 원티드 이력서 탭에 없어서」** 다.

### 무엇이 나왔나

| | 값 |
|---|---|
| **원티드 15건** | 전부 **원티드 자체 지원**(`out_link: null`, 대조군으로 필드 동작 확인). **서술형 문항 없음**, 글자수 캡 없음. 첨부는 슬롯이 아니라 **[이력서] 탭 파일 목록의 체크박스 최소 1개** |
| **마카롱팩토리** | 리멤버를 열지 않고 닫았다 — 회사 공식 ATS 가 **그리팅 공개 폼**이다. 이력서 필수 1 + 선택 3(경력기술서·포트폴리오·GitHub) + 🔴 **자기소개서 700자 필수**(미제출 시 서류 불합격) + 지원경로 드롭다운 필수 |
| **와탭랩스** | 리멤버 자체 지원. 제출물은 **이력서 PDF 하나**. 🔴 전형 2단계가 **라이브 코딩테스트**, 뒤에 **평판조회** |
| **컷 17건 확정** | AI Engineer 6 · Platform Engineer 6 · FDE 1 · Embedded SW 4 |

폼 구조 정본: **`applications/WANTED-APPLY-FORM.md`** (원티드 15건이 공유).

### 🔴 GLG 가 해야 하는 한 걸음

**원티드 [이력서] 탭 실측 (2026-09-18, `wanted.co.kr/cv/list`)**

| | |
|---|---|
| 기본 이력서 | 1건 · 작성 완료 · **26.08.03** |
| 그 밖 | 완료 1(25.10.02) · 작성 중 2(26.07.24 · 25.05.14) |
| **09-18 재빌드 컷 PDF** | **0건** |

지금 이 상태로 원티드에서 내면 **8월 3일자 기본 이력서**가 나간다 — `platform-engineer` 신설 ·
FDE 컷 축 정정 · 자가 측정 수치 제거가 **전부 안 실린다.**

⛔ 에이전트가 올리지 않는다(계정 자산 변경). **GLG 가 `resume/build/` 의 컷 PDF 를 올린다.**
올리는 순간 원티드 15건이 한꺼번에 `ready` 가 된다.

### 함께 확인해 줄 것 — 한 번이면 15건이 닫힌다

아무 원티드 건에서 지원 모달을 **한 번만** 열어 셋을 확인한다(에이전트는 🔴 「지원하기」를
클릭하지 않으므로 모달 내부만 미확인으로 남아 있다).

1. 체크박스 목록 말고 **별도 파일 업로드 버튼**이 모달 안에 있는가
2. **서술형 입력 칸**이 하나라도 있는가 (있으면 글자수 캡)
3. 프로필 자동 외에 **손으로 채울 칸**이 있는가

## 👤 GLG 판정 대기 — 폼 실측에서 새로 올라온 것

1. 🔴 **애자일소다 — 위즈코어와 같은 구조인데 판정이 다르다.** 담당업무는 K8s 시스템인데
   **자격요건은 FastAPI+React 풀스택**이고(직접 증거 1/3), `platform-engineer` 컷이 명시로
   부인하는 K8s 운영을 JD 가 요구한다. 위즈코어는 `held`, 이 건은 `draft`. 임의로 안 내렸다.
2. 🔴 **마카롱팩토리 — 판정이 내려갔다.** 기존 매트릭스가 **담당업무를 필수요건 자리에** 놓아
   「선행 큐(직접 증거 2건 이상)」로 적혀 있었다. 자격요건 원문으로 다시 세우니 직접 증거 1개 =
   **`reach`**. 게다가 자격요건 1·2번(AWS 프로덕션 운영 · Terraform/CDK)이 컷 부인 범위다 —
   **애자일소다·위즈코어와 같은 계열**이다.
3. **심플랫폼 — 연차 하한 9년, `FAQ.md` §3 의 폼 값은 8+ years.** hard gate 로 먼저 잘릴 수 있다.
   반대로 **ATCI 는 상한 5년**이라 「경력이 많아서」 걸릴 수 있다.
4. ⚠ **reach 가 6건으로 불었다** — 미리비트(Scala 필수 없음) · 애자일소다 · 링크알파(영어 필수) ·
   멘타트 · 카이트다이나믹스 · 리주비놀. `AGENTS.md` §둘째 축은 **2~3건 제한**이다. 로봇 2건은
   2026-09-18 GLG 판정(「피지컬AI 로봇쪽도 임베디드 주제니까 넓게 보면 내가 할게 있을거야」)을
   reach 사유로 적어 두었다.
5. **마카롱팩토리 자기소개서 700자 초안** — `FAQ.md` §9 골격을 쓰되, 이 칸은 **동기**를 묻으므로
   §9 판정 기준대로 **경계 문단은 넣지 않는다.** 초안을 에이전트가 써도 되는지 GLG 결정.
6. **와탭랩스 라이브 코딩테스트** — `FAQ.md` §11(2026-08-04 override) 적용 대상.
7. **`AGENTS.md`·`FAQ.md` 에 내려야 할 계약 두 개** (이번 세션은 두 파일을 고치지 않았다):
   - **「매트릭스는 담당업무가 아니라 자격요건에서 뽑는다」** — 마카롱팩토리에서 판정이 한 칸
     부풀려진 원인이고, 같은 오류가 다른 건에도 있을 수 있다.
   - **`FAQ.md` §10 에 「원티드 자체 지원 경로에는 서술형 칸이 없다」** — §9 단골 답을 어느 채널에서
     쓰는지 가르는 값이다.

## 🆕 알파칩스 — 이 배치와 성격이 다른 건 (2026-09-18 추가)

리멤버 포지션 제안을 GLG 가 수락해 **JD 를 메일로 받은 건.** 공개 공고 URL 이 없고
지원 폼도 없다 — **이메일 회신으로 제출**한다.

- **시스템 축 ✅** — ARM/RISC-V Custom Silicon 의 Bootloader·BSP·Device Driver·SDK.
  요건 매트릭스 **선행 큐 후보**(직접 증거 2 + 재직 사실 1).
- 🔴 **`resume/` 파이프라인 밖을 요구한다** — **지정 `.docx` 양식 이력서 + 경력기술서**.
  빌드된 PDF 컷을 그대로 낼 수 없다. `dossier/` 자산을 쓸지 GLG 판단이 필요하다.
- 🔴 **연봉 칸이 있다** — 현재(기본급/성과급/기타 분리) + 희망. **GLG 만 채운다.**
  `FAQ.md` §4 에 없는 새 유형이라 추천 경로에서 반복될 질문이다.
- **시간**: 마감은 없지만 **추석 연휴 전 제출 시 9/28 주에 바로 서류 검토**가 시작된다.
- 경계는 `body.org` grep 으로 세웠다 — 부트체인·커널 드라이버·JTAG·PCIe/DMA·Secure Boot **0건**.
  포팅·통합·제품화 경력으로 bring-up 자리에 서는 건이다. 숨기지 않고 낸다.
- ⚠ 제안을 보낸 쪽의 이름·연락처·소속은 **기록하지 않는다**(GLG 지시). 원본 `.docx` 도 저장소에 없다.

## 🟡 이전 판정 대기 (그대로 남아 있음)

- **모레 2건** (2026-09-09 `rejected`, 기본값 제외) · **OpenAI FDE↔DevEx**(DevEx 가 `ready`) ·
  **마키나락스**(담당자 추천 경로가 이미 열려 있다) · **위시드를 선행 큐에 넣을지**.
- **`MATERIAL.md`** — 프로필 repo `junghan0611/junghan0611` 의 `apply/`. ⚠ K8s·모델 서빙 기록이
  거기 있으면 `platform-engineer` 컷의 경계 문장과 위 1·2번 판정이 **달라진다.**
- **「글」이 무엇인지 지목** — 가든 코어 글 / ax 서사 / 지원서 자기소개 문단.

## Verify

```bash
applications/check.py --deep
applications/alive.py
./scripts/check-public.sh --repo .
(cd resume && ./run.sh all)          # all 이 verify 까지 부른다
```

## Read

`.claude/skills/apply/SKILL.md` — **먼저 읽는다.** §4 리멤버 오클릭 금지, §3 제출 직후 3-way 닫힘,
§8 이 저장소에 없는 것. `applications/WANTED-APPLY-FORM.md` — 원티드 폼 정본.
`AGENTS.md` §시스템 축 · §둘째 축 · §인용은 열어 보고 적는다.
`applications/FAQ.md` §원칙 여섯 · §9 서술형 단골 답 · §10 첨부 파일 기본값 · §11 코딩테스트.

## Do not touch

🔴 **「지원하기」·「간편 지원하기」·「입사지원」·「제출하기」 를 어느 화면에서도 클릭하지 않는다.**
리멤버 URL·엔드포인트 패턴을 저장소에 적지 않는다. 보상값을 저장소에 적지 않는다.
제출 완료 건의 `submit/` 스냅샷을 재빌드·덮어쓰지 않는다. 커밋·푸시는 GLG 요청 시에만.

# RECENT

- [2026-09-16] Gmail 대조로 리멤버·원티드 6건을 사후 회수하고 2건을 `rejected` 로 닫았다 (`860e2bd`).
- [2026-09-18] **원티드 45건 생존 실측** — 34 active / 9 close / 2 draft. 9 일 기다리는 사이
  **에스투더블유(S2W) FDE(AI Platform)** 가 닫혔다. 09-09 배치에서 「GLG 축과 정면」 1번이었다.
- [2026-09-18] **`AGENTS.md` §시스템 축 신설.** offense mode 안에서 FDE·AI·AX 를 다시 가른다 —
  런타임·게이트웨이·드라이버를 만드는 자리는 내고, 고객 요청·사내 서비스를 구현하는 자리는 뺀다.
- [2026-09-18] 소넷 형제가 원티드 34 + 리멤버 10 을 **담당업무 원문 인용**으로 재분류했다.
  판정이 뒤집힌 것들 — 스켈터랩스 FDE(⭐⭐정면 → 서비스), 클리카(시스템 예상 → 서비스),
  위즈코어·마이클(제목만 확인 → 시스템). **제목으로 판정하면 반대로 고른다**는 증거다.
- [2026-09-18] **모레 AI Agent System Engineer(R 329027) 마감 확인.** 09-09 리멤버 배치 1번이었다.
  S2W 에 이어 9 일 사이 잃은 두 번째 정면 건이다.
- [2026-09-18] **원장 대조 누락을 계약으로 닫았다** — 인핸스(`rejected`)가 후보로 다시 올라온
  사고. `AGENTS.md` §후보를 판정하기 전에 원장을 먼저 본다.
- [2026-09-18] **폼 실측 17건 완료.** 원티드 15건의 지원 경로를 공개 API `out_link` 로 확정하고
  (대조군 없이 `null` 을 믿지 않았다), 폼 구조를 `applications/WANTED-APPLY-FORM.md` 한 벌로
  분리했다. **리멤버 2건은 로그인 화면을 열지 않고 닫았다** — 마카롱팩토리는 회사 공식 ATS 가
  그리팅 공개 폼이었고, 와탭랩스는 공개 색인된 공고 본문이 제출 서류·전형을 다 적고 있었다.
  ⭐ **「폼 문항을 모른다」가 병목이 아니었다** — 원티드 15건엔 서술형 칸 자체가 없고, 진짜
  병목은 **이력서 탭에 컷 PDF 가 0개**라는 것이었다.
- [2026-09-18] **`FAQ.md` 원칙 5·6 추가.** 검증을 부르는 문서 / 애매한 수치 전면 제외
  (전화 면접에서 토씨 하나로 추궁당한 실측이 근거).
