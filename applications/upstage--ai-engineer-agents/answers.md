# 서술형 답 — 업스테이지 AI Engineer - Agents

폼의 필수 서술형은 6개다(`submission.md` §폼 실측). 이 파일이 그 답의 정본이고,
**붙여넣기 직전에 폼 카운터(`0/1000`)를 눈으로 확인한다** (`FAQ.md` §글자 수 세 가지 셈법).

| 문항 | 상한 | 상태 |
|---|---|---|
| 1. 제품 체험 평가 | 10,000 | ✅ **초안 작성 완료 — 아래 문항 1** (실측 SSOT: `~/repos/gh/memex-kb/scanpdf/work/물리학강의/upstage/README.md`) |
| **2. 업무 경험** | **1,000** | ✅ **아래 완성** |
| 3. 관심 분야 (다중선택) | — | ✅ `Data Collection & Processing` + `Natural language processing` |
| 4. 선호 언어 (선택형) | — | ✅ `Python` (선택지에 Go 가 없다) |
| 5. 합류 가능 시점 | 1,000 | ✅ 「협의 가능」 · 날짜 강제 시 「최종 합격·처우 협의 후 4주 이내」 |
| 6. 지원 경로 | 1,000 | ✅ 「채용 홈페이지」 |

---

## 0. 폼 전체 입력 순서 — GLG가 직접 채울 때 이 표만 위에서 아래로 따라간다

`/ko/o/194880/apply/new` 실제 구조(2026-07-31 브라우저 실측, `submission.md` §폼 전체 구조 실측이 근거).
**중간 저장이 안 되므로 한 번에 끝까지 간다.** 문항 텍스트는 아래 §1·§2·§5·§6에서 그대로 복사.

### 기본정보 — 본인 값 (여기 적지 않음)
이름·성별·생년월일·이메일주소·연락처 — 이름은 **김정한**, 이메일은 **[email removed]**.
나머지(성별·생년월일·연락처)는 브라우저에서 직접.

### 병역/취업우대
- 병역사항: 본인 값 직접
- 장애여부: **비대상** (`FAQ.md` §5 「해당 없음」)
- 보훈여부: **비대상** (`FAQ.md` §5 「해당 없음」)

### 경력사항 — 「항목 추가」로 3건 (고퀄·성균관대·NEMO-UX, 2026-07-31 GLG 확정)

🔴 **2026-07-31 GPT 교차검수 P0** — 아래 표 중 **부서·직급·정확한 월**은 이 폼이 요구하는
정밀도로 SSOT에 없다. `FAQ.md` §3 은 「법인 2년 + 앞뒤 준비 2년」·「정확한 월·일은 공단
가입이력증명서로만 확정」이라고 스스로 말한다. **없는 정밀도를 만들어 채우면 「입사지원 서류에
허위사실이 발견될 경우 영입 취소」 조항에 그대로 걸린다.** ⚠ 표시가 붙은 칸은 **GLG가 실제
값(또는 공단 증명서)을 보고 직접 정한다** — 에이전트가 추정으로 채운 값을 그대로 옮기지 않는다.

**항목 1 — GoQual Inc.**
| 칸 | 값 |
|---|---|
| 고용형태 | 정규직 |
| 회사명 | `GoQual` 입력 → 「+ 직접 입력하기: "GoQual"」 클릭 (검색 결과 없음이 정상) |
| 근무기간 | 입사일 **2025.06** · 「재직 중」 체크 (퇴사일 칸 비움) — `FAQ.md` §3 확정값. 일 단위를 요구하면 **2025.06.24** (오토에버 실측값) |
| 부서 | ✅ **개발팀** — 오토에버 제출 실측값. (추정이었던 것이 GLG 직접 입력으로 확정됐다) |
| 직급 | ✅ **프로** — 오토에버 제출 실측값. ⚠ `Full Stack Architect` 는 **이력서 표제이지 사내 직급이 아니다** — 「직급」 칸에는 사내 호칭인 **프로**를 넣고, 역할명은 담당업무 쪽에서 말한다 |
| 담당업무 | 에이전트 워크서피스(VOC·인시던트·Forge 연동) 설계·운영, Zigbee 허브 펌웨어(Zig) 양산화, 서버(Go)·앱(Flutter) 직접 개발, 온디바이스 AI 스마트홈 허브 개발 — `resume/body.org` 공개 문구 그대로 |

**항목 2 — 성균관대학교**
| 칸 | 값 |
|---|---|
| 고용형태 | ✅ **계약직** — 오토에버 제출 실측값 (추정이던 「기타」는 틀렸다) |
| 회사명 | `성균관대학교` 입력 → 직접 입력하기 (또는 검색 결과 뜨면 그걸 선택) |
| 근무기간 | ✅ **2017.08 – 2022.07** (5년) — 오토에버 제출 실측값. 🔴 `FAQ.md` §3 의 「2018 – 2022.06」은 **이 실측값과 어긋난다**. NEMO-UX 법인 종료(2017.08) **직후 바로 연구실로 이어진 것**이라 사이에 공백이 없다 |
| 부서 | ✅ **분산컴퓨팅연구실** — 오토에버 제출 실측값 |
| 직급 | ✅ **박사과정연구원** — 오토에버 제출 실측값 |
| 담당업무 | 비휘발성 메모리 파일시스템·NUMA 락 연구. Virginia Tech COSMOSS Lab 방문연구(2019.07–2020.03) 포함. 박사 학위는 미취득(수료) — 이 문장은 `FAQ.md` §3 확정 사실 |

**항목 3 — NEMO-UX**
| 칸 | 값 |
|---|---|
| 고용형태 | **정규직** — 오토에버 제출 실측값 (에이전트가 추정했던 「개인사업」은 틀렸다) |
| 회사명 | `NEMO-UX` 입력 → 직접 입력하기 |
| 근무기간 | ✅ **2015.08 – 2017.08** (2년 1개월). 🔴 **2013 을 넣지 않는다** — 이 칸이 묻는 것은 **법인 재직 기간**이고, 2013–2015.08 은 그 **앞의 준비 기간**(박사과정 병행)이라 재직이 아니다. 이력서의 `2013 – 2017` 과 값이 달라 보이는 것은 **의도된 것**이다 (2026-08-01 GLG 확정 · `FAQ.md` §3 §NEMO-UX 는 값이 둘이다) |
| 부서 | **부서 미제출** — 오토에버 제출 실측값 |
| 직급 | **공동창업자** — 오토에버 제출 실측값 |
| 담당업무 | 리눅스 기반 대형 터치 디스플레이 OS 공동창업, 개발 총괄. 사업은 종료(실패) — 감추지 않는다(`FAQ.md` §3) — 이 문장은 확정 |

### 제출서류
| 칸 | 파일 |
|---|---|
| 이력서 (필수) | `resume/build/KimJunghan_Resume_AI_Engineer.pdf` |
| 자기소개서 (선택) | `dossier/build/KimJunghan_AX_Competency.pdf` (경력·역량기술서 5쪽 — 전용 칸이 없어 이 칸에 낸다) |
| 포트폴리오 (선택) | 라디오 「파일」 선택 → `dossier/build/KimJunghan_AX_Portfolio.pdf` (12쪽) |

### 추가질문 (서술형은 §1·§2·§5·§6, 아래는 선택형만)
- 관심 분야: **Data Collection & Processing** · **Natural language processing** 체크
- 선호 언어: **Python** 라디오
- GitHub URL: `https://github.com/junghan0611`
- LinkedIn URL: `https://linkedin.com/in/junghan-kim-1489a4306`
- 강연/세미나/소개영상: 비움

### 지원정보 (추천인 정보)
전부 비움 — 이 건은 추천 경로가 아니다.

### 개인정보 수집 및 이용 동의 — 순서대로 체크
1. **개인정보 필수항목 동의 (필수)** — 체크
2. **개인정보 선택항목 동의 (선택이지만 반드시 체크)** — 🔴 **안 하면 방금 넣은 경력사항·이력서·포트폴리오·지원분야·지원경로가 전부 처리되지 않는다** (`submission.md` §③)
3. **민감정보 수집 및 이용 동의 (필수)** — 체크
4. 개인정보 제3자 이용제공 동의 (㈜두들린, 선택) — GLG 판단, 안 해도 지원 가능

### 마지막
`제출하기` 버튼은 위 전부 확인 후 GLG 가 직접 누른다.

---

## 문항 1 — 업스테이지 제품 체험 평가 (필수 · 10,000자)

```text
저는 모델 제품을 응답 한 번의 성능으로만 평가하지 않습니다. 실제 조직의 문서·데이터·도구와 만났을 때 모델이 무엇을 읽고, 어디까지 행동하며, 실패와 판단을 누구에게 돌려줄 수 있는지를 함께 봅니다. 그래서 이번 체험도 Solar를 제 에이전트 하네스에 넣는 일과, Document AI를 실제 문서 파이프라인에 놓는 일을 한 흐름으로 진행했습니다.

Solar Open 2는 현재 제 계정에 호출 권한이 없어 평가 대상에서 제외했습니다. 대신 공개된 Solar Pro 3를 Pi에 직접 붙였습니다. 기본 provider가 없어서 `agent-config/pi-extensions/upstage-provider.ts`로 `api.upstage.ai/v1`을 OpenAI-compatible provider로 등록했습니다. OpenAI 호환이라는 표기만으로 agent가 그대로 올라가지는 않았습니다. `store`, developer role, strict tool schema, reasoning effort, streaming usage, prompt cache처럼 하네스가 기대하는 런타임 계약을 하나씩 대조해야 했습니다. 모델 호출을 성공시키는 것과, 다른 도구·세션·사람의 판단 사이에서 오래 쓰는 실행면에 넣는 것은 다른 일이라는 점을 이 통합에서 확인했습니다.

이 경험은 업스테이지가 제공하는 개발자 표면을 읽으면서 더 선명해졌습니다. 공식 문서는 Claude Code와 Hermes Agent를 Solar provider로 등록하는 통합 경로와, 코딩 에이전트가 읽을 원문 API 문서를 함께 제공합니다. 제가 손으로 provider 경계를 확인한 일과 회사가 제품으로 열어 둔 길이 같은 질문을 향합니다. 중요한 것은 원클릭 연결 자체가 아니라, 서로 다른 하네스에서도 모델·도구·기억· 권한의 소유 경계를 잃지 않으며 통합을 운영하는 일이라고 봅니다. Solar Open 2의 긴 문맥 지향도 기술보고서로 읽었지만, 접근하지 못한 모델의 성능을 제 경험인 양 말하지는 않겠습니다.

Document Parse는 제가 운영해 온 스캔책 파이프라인의 기존 OCR 비교축에 추가했습니다. 기존 기록은 MinerU, PP-StructureV3, DeepSeek-OCR, PaddleOCR-VL의 모델/도구 모드, GLM-OCR을 같은 한국어 책 구간에서 비교한 것이고, 이번에는 최무영 《물리학 강의》의 동일 구간에 Upstage Document Parse를 놓았습니다. 이번 직접 대조는 MinerU 중심이며 gold 대조 벤치마크는 아닙니다. 따라서 아래 판단은 제품의 절대 순위가 아니라, 제가 실제로 운영하는 파이프라인에 무엇을 어떻게 배치할지에 대한 운용 판정입니다.

글자 층에서는 고유명사가 강했습니다. 기존 엔진들이 서로 다른 자리에서 깨뜨렸던 톰슨·돌턴· 슈뢰딩거·찐빵을 이번 출력은 함께 보존했고, MinerU가 이 구간에서 만든 한자 환각도 없었습니다. 반면 구조 층은 문서 유형에 민감했습니다. 수식과 2단 도판이 밀집한 구간에서는 화학반응식이 `equation`이 아니라 `heading1`로 분류되고, 그림 캡션이 본문 문장 한가운데를 갈라 reading order가 무너졌습니다. 수식 없는 인문서 구간에서는 소제목·각주·목록·표가 안정적으로 분리됐습니다. 그래서 산문 문서는 Document Parse를 중심으로 두되, 수식·도판이 많은 과학책은 기존 도구가 구조 골격을 맡고 Upstage OCR이 글자 검수에 들어가는 조합이 맞다고 판단했습니다.

실패도 운영 규칙으로 남겼습니다. 화살표와 개념 라벨뿐인 칸트 도식을 막대차트로 오인해 원문에 없는 수치가 든 표를 생성한 사례가 있었습니다. 도식이 있는 문서에서는 `chart_recognition`을 끄고, 생성 결과는 정본에 바로 넣지 않고 검수 경로를 지나게 해야 합니다. 없는 숫자가 지식베이스에 섞이는 일은 글자 한두 개의 오독보다 훨씬 되돌리기 어렵기 때문입니다.

같은 문서 흐름에서 Document OCR, Information Extraction, Classification도 각각의 자리를 확인했습니다. OCR의 단어별 confidence와 bounding box는 여러 엔진의 불일치로 찾던 오독 후보를 검수 큐로 보내는 데 맞았고, Information Extraction은 판권지에서 누락된 ISBN까지 메타데이터로 채웠습니다. Classification은 쪽 유형 자체는 유용했지만 쪽과 그룹의 대응을 얻지 못해 본문·찾아보기 경계를 자동으로 정하는 용도는 보류했습니다. 제품군을 한 덩어리로 칭찬하는 대신, 문서 유형과 작업 단계에 따라 어느 API를 어느 검증 경계에 둘지 나누는 것이 더 실무적인 사용법이라고 봅니다.

이 경험을 통해 제가 보고 있는 제품의 자리가 분명해졌습니다. Document AI는 OCR 정확도 순위가 아니라, 비정형 문서를 모델과 agent가 읽고 행동할 수 있는 구조로 바꾸는 입구입니다. 그 뒤에는 문서 유형별 평가, 검색 입력 품질, 근거를 남기는 장기 실행 workflow, 그리고 사람이 예외와 최종 판단을 소유하는 작업면이 이어져야 합니다. 저는 문서 구조화 → OCR 검증 → Org/EPUB·검색 색인 적재를 역할이 분리된 agent workflow로 연결해 왔습니다. 업스테이지에서라면 모델의 능력을 실제 업무의 문서·도구·결정으로 옮기는 이 경계 — 제품 평가를 고객의 운영 규칙과 재현 가능한 agent 경험으로 바꾸는 일 — 을 함께 만들고 싶습니다.
```

**근거** — `~/repos/gh/memex-kb/scanpdf/work/물리학강의/upstage/README.md` (private `scanpdf/`).
Solar provider 실측은 `~/repos/gh/agent-config/pi-extensions/upstage-provider.ts` 와
`https://github.com/junghan0611/agent-config/issues/17`.

**글자 수 (2026-07-31 재측정)** — 줄바꿈 제외 **2,782자 / 10,000자.**

✅ **2026-07-31 교열·구조 정리 반영**
- `scanpdf`의 직접 대조 범위(MinerU 중심·gold 대조 없음)를 본문에 밝혀, 결과를 절대 벤치마크가
  아니라 문서 유형별 **운용 판정**으로 썼다.
- 자가 측정 성능·비용 수치는 빼고, 고유명사 보존·구조 오분류·차트 환각처럼 실제 운영 결정을
  바꾼 사례를 남겼다.
- 공식 Claude Code·Hermes Agent 통합과 Solar Open 2 기술보고서는 제품 성능의 근거로 쓰지 않았다.
  대신 **모델을 여러 agent 하네스의 실제 작업면으로 옮기는 문제**라는 제품·직무 맥락으로만
  배치했고, 미접근 Open 2는 평가하지 않았다고 경계를 밝혔다.
- 이번 제출 패킷의 공통 명제인 **「모델 역량을 문서·도구·사람의 판단이 만나는 작업면으로 옮긴다」**를
  마지막에 명시해, 제품 체험이 단발 사용기가 아니라 Agent Engineering 제안으로 닫히게 했다.

---

## 문항 2 — 지원 분야와 관련된 업무 경험 (필수 · 1,000자)

```text
제가 해 온 일은 에이전트를 인상적인 데모가 아니라 매일 쓰는 도구로 굴리는 것이었습니다.

운영. AI 에이전트 하네스를 직접 만들어 상시 운영합니다. 상류가 깨질 때마다 그것을 운영 규칙으로 번역했고, 스킬 단일 정본 하나를 여러 하네스에 배포해 한 곳을 고치면 전부가 따라오게 했습니다.

워크플로와 프로토콜. 서로 다른 하네스가 상대의 대화기록·인증·런타임을 소유하지 않고 식별자로 서로를 주소하는 협업 기층을 설계해 공개했습니다. 설계 주장에 그치지 않았다는 근거는 제3자입니다. 만난 적 없는 개발자가 다른 백엔드를 붙이겠다며, 제가 확장점이라고 말한 바로 그 자리를 찾아 들어와 11개 파일 885줄을 기여했습니다.

문서와 검색. 공용 GPU도 임베딩 기반도 없던 자리에 클러스터와 데이터 파이프라인을 깔고 사내 문서 검색을 직접 서빙한 뒤, 담당자에게 붙는 도메인 에이전트를 세웠습니다. 개인 작업에서는 HWPX·스캔 문서를 Org와 검색 색인으로 연결하는 파이프라인을 운영했고, 이번 Upstage Document Parse 평가도 그 워크플로 위에서 수행했습니다. Document AI가 본진인 회사라 이 축이 가장 가깝습니다.

경계도 밝힙니다. 관측 설계는 하네스 로그와 세션 축 수준이지 OpenTelemetry 급이 아닙니다. 모델 서빙·경량화 실적이 없고 VLM은 얇습니다. Rust는 제 축이 아닙니다.

확인 경로: github.com/junghan0611 · ax.junghanacs.com · notes.junghanacs.com
```

**글자 수 (2026-08-01 `ax.junghanacs.com` 추가 후 재측정)** — 세 셈법 전부 상한 1,000 아래다.
아래 값은 **위 ```text 블록에서 기계로 센 것**이고 `submit/answers.txt` 머리글과 같은 수다.

| 셈법 | 값 |
|---|---|
| **공백·줄바꿈 포함** (가장 불리) | **764** |
| 줄바꿈만 제외 | 754 |
| 공백 전부 제외 | 599 |

> 2026-07-31 판은 739 · 724 · 581 이었다. 「확인 경로」에 **ax 증거면 한 자리를 더한 차이**다.
> 폼 카운터 실측값도 **764** 로 같다.

**왜 이 순서인가** (`FAQ.md` §9 골격):
운영 → 프로토콜 → 문서·검색 순으로 **공고의 담당 업무에 1:1로 붙였다.** 마지막 문단은
경계이고, 이 문단이 나머지 전부의 신뢰를 만든다. **「봐 주세요」라고 쓰지 않고 좌표만 놓는다.**

⚠ **회사 실명·고객 식별자를 쓰지 않는다** — 「사내」로 처리했다.

✅ **2026-07-31 GPT 교차검수 반영** — 운영 문단의 자가집계 수치(「20여 개 버전」·「스킬 40여
종」·「여섯 개 하네스」)를 뺐다. 이 배치 다른 문서에서 이미 벌어진 「하네스 5 vs 소비표면 6」
정의 문제를 이 폼에 다시 불러올 위험이 있었다. 문서·검색 문단은 OCR 5엔진/17쪽 설명이
문항1과 그대로 겹쳐서 「HWPX·스캔 문서 → Org·검색 색인」 한 줄로 줄이고, Document Parse
평가는 그 워크플로에서 나온 것으로만 연결했다. 경계 문단(OpenTelemetry·모델 서빙·VLM·Rust)은
JD 필수 항목에 대한 정직한 답이라 그대로 유지.

---

## 문항 5 — 합류 가능 시점 (필수)

```text
협의 가능합니다. 현재 재직 중이라 인수인계가 필요하며, 최종 합격과 처우 협의 이후 4주 이내 합류를 기준으로 보고 있습니다.
```

`FAQ.md` §4 확정값이다 — 희망 연봉과 같은 자리이므로 여기서 새 문장을 만들지 않는다.

---

## 문항 6 — 지원 경로 (필수)

```text
업스테이지 공식 채용 페이지에서 직접 찾았습니다. 다만 이번에 처음 알게 된 회사는 아닙니다. 2025년 10월에 이미 업스테이지 채용 페이지(커리어 토크)를 북마크해 두었고, 그 전부터 Solar API와 문서 AI 제품을 제 작업에 붙여 써 왔습니다.
```

✅ **2026-08-01 GLG 판단 — 붙인다.** 「즉흥이 아니다」를 사실로 세우는 한 줄이다.
근거는 `submission.md` §근거가 이미 있다 의 서지 표 — **2025-10-01 `upstage.ai/careers/careertalk-sep25`
북마크**(`@web-sesangeulbagguneunaireul`). 날짜·인용 키가 있으므로 요청받으면 그대로 제시한다.
⚠ 개월 수는 적지 않는다 — 시간이 지나면 틀리는 숫자다.
