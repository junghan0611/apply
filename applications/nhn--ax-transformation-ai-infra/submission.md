# 제출 기록 — NHN / AX Transformation · AI Infra (BE)

| 항목 | 값 |
|---|---|
| 상태 | **submitted** |
| 지원일 | **2026-07-20** |
| 경로 | NHN 자체 채용홈 + **그룹사 직원 추천** (`referral.md`) |
| 첨부 컷 | **전용 dossier 3종** — 이력서 컷을 쓰지 않은 **유일한 건** |
| 공고 | `JD.md` — 본문 **일부**(옮겨 적은 판) |
| 근무지 | 판교 삼평동 플레이뮤지엄 |

> **이 저장소의 첫 지원이다.** 표준 구조(`JD.md` · `submission.md` · `submit/`)가 생기기
> 전에 낸 건이라 산출물이 `dossier/` 한 폴더에 통째로 있었다. **2026-07-29 에 표준으로 옮겼다** —
> 아래 §구조 이관.

## 낸 것 — `submit/` 이 증언한다

폼의 **포트폴리오 제출란**에 3종을 올렸다. 파일 타임스탬프가 전부 `2026-07-20 11:41~11:42` 다.

| 파일 | 크기 | 무엇 |
|---|---|---|
| `submit/KimJunghan_AX_Competency.pdf` | 329KB | JD 관련 역량·성과 기술서 |
| `submit/KimJunghan_AX_Portfolio.pdf` | 3.8MB | 이미지·프로젝트 중심 포트폴리오 |
| `submit/KimJunghan_AX_Detail.md` | 60KB | 상세 증거 Markdown (공고가 md 를 명시 요구) |
| `submit/KimJunghan_AX_Self_Introduction.md` | 2.4KB | **자기소개 — 폼 입력란에 붙여 넣은 글** (11:55 작성) |
| `submit/MANIFEST.sha256` | — | 위 4종 + 증거 패키지 ZIP 의 해시 |

**증거 패키지 ZIP(15.9MB)은 git 에 담지 않고 해시만 남겼다.** ZIP 의 내용물은
`../../dossier/` 에 **이미 전부 추적되고 있는** org 원본·이미지·파이프라인이라, 바이너리를
또 넣는 것은 같은 것을 두 번 두는 일이다. 무엇을 냈는지는 `MANIFEST.sha256` 의
`eed28831…` 가 증언한다. 실물은 `dossier/build/` 에 로컬로 남아 있다(gitignore).

## 왜 이 직무였는가

공고 제목은 「AI 인프라(백엔드)」였지만 **주요 업무 절반이 문서·배포·교육**이었다.
그래서 **빌드 + 전파의 이중 직무**로 읽고 거기에 조준했다. 이 읽기가 이후 이 저장소의
조준 방식이 됐다(`../AGENTS.md` §직무 선택). AIRS Medical·한화비전 건이 같은 구조다.

## 구조 이관 (2026-07-29) — 이 건이 표준에서 벗어나 있었다

GLG: *"제출 폴더를 이제 우리 표준 방식으로 맞출 거야. 처음이라 이렇게 해 본 거고
이제 표준 방식으로 잡자."*

옛 `dossier/` 한 폴더에 **두 가지가 섞여 있었다.** 그것을 갈랐다.

| 무엇 | 어디로 | 왜 |
|---|---|---|
| 문서 **생성기** (org 원본 3종 · `references.bib` · `images/` · `pipeline/` · `run.sh` · `flake.nix`) | **`../../dossier/`** | 회사 이름이 붙을 물건이 아니다. `resume/` 와 같은 **발사대**이고 한화비전·NHN 2번째 건이 이미 재사용을 예정하고 있다 |
| 이 **한 건의 제출 기록** (공고 · 추천 · 실제 낸 파일) | **여기** | 표준 `<company>--<role>/` |
| 루트 `NHN.md` | → `JD.md` + `referral.md` | 공고 원문과 추천을 갈랐다 — `../AGENTS.md` 는 JD.md 를 **원문 전용**으로 못 박는다 |

**고쳐진 실질적 결함이 하나 있다.** 옛 구조에서 `dossier/build/` 는 **gitignore** 였다.
즉 **실제로 낸 파일이 git 에 없었다** — `../AGENTS.md` 의 「`submit/` 은 git 으로 추적한다 …
무엇을 냈는지가 이 저장소의 존재 이유다」를 이 건만 지키지 못하고 있었다. 이제 지킨다.

**원장의 예외도 없어졌다.** `check.py` 는 폴더가 `../` 로 시작하는 행을 건너뛴다
(「닫힌 외부 레인은 자기 검증 계약을 쓴다」). 이 건이 그 유일한 이용자였고, 이제
**26건 전부가 같은 검사를 받는다.**

## 이후 기록

- [2026-07-20] **제출 완료.** 3종 업로드 + 자기소개 입력. 개인·법적 필드는 GLG 가 브라우저에서
  직접 입력했고 **값은 저장하지 않는다.**
- [2026-07-29] **표준 구조로 이관.** `submit/` 스냅샷을 git 에 넣고, dossier 발사대를 분리했다.
  회신은 아직 없다.
