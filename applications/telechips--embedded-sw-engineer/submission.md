# 제출 기록 — Telechips (텔레칩스) / [판교 사옥] Embedded S/W Engineer

| 항목 | 값 |
|---|---|
| 상태 | **ready — 보류** (서류는 준비돼 있다 · **2026-08-01 GLG: 임베디드 컷으로 내지 않는다**) |
| 지원일 | — |
| 경로 | **자체 채용홈** `careers.telechips.com` (ninehire 화이트라벨) — 가입 불필요 |
| 첨부 컷 | **Embedded** (`resume/build/KimJunghan_Resume_Embedded_Software_Engineer.pdf`, 2쪽) |
| 추가 첨부 | **`submit/KimJunghan_Embedded_Experience_Detail.pdf` (3쪽)** — 폼 11번 **포트폴리오(필수)** 칸. `./build.sh` 로 재현. **`stage.py` 가 옮기지 않는다**(그 정규식은 `resume/build`·`dossier/build` 만 읽는다) — DEEPX 선례대로 `build.sh` 가 `submit/` 에 직접 놓는다 |
| 공고 URL | https://careers.telechips.com/job_posting/iSXmKnRT |
| **지원 URL** | https://careers.telechips.com/job_posting/iSXmKnRT/apply |
| 근무지 | **텔레칩스 판교사옥** — 경기 성남시 수정구 금토로80번길 27 |
| 조건 | 정규직 · 경력 **3~15년** · 마감 **상시** · 직군 `Automotive Embedded S/W` |

## 낸 것

- [ ] **이력서 PDF** — `resume/build/KimJunghan_Resume_Embedded_Software_Engineer.pdf`
      (절대경로: `~/repos/gh/apply/resume/build/KimJunghan_Resume_Embedded_Software_Engineer.pdf`)
- [ ] 커버레터 / 지원 동기 — 폼에 칸이 있으면 아래 §왜 이 직무였는가 를 줄여 쓴다
- [ ] 추가 서류 — 폼을 열어 확인. **경력기술서를 따로 받으면** DEEPX BSP 건의 통합본 선례를 쓴다
      (`../deepx--linux-bsp-engineer/`)

**⚠ ninehire 는 파일 업로드를 에이전트가 못 한다.** Enhans 건에서 확인했다 — file input 이 DOM 에
없고 클립 버튼이 네이티브 대화상자를 연다. **GLG 가 직접 올린다.** 올린 파일은 `submit/` 에 복사.

> **⛔ dossier 깊이 문서(AX Competency · Portfolio)를 붙이지 않는다 (2026-07-30 판정).**
> 2026-07-30 에 AX·에이전트 축 11건에는 이 2종을 세트에 깔았지만 **이 건은 뺐다.**
> 이 건의 컷은 **Embedded** 이고, 두 문서는 AX 전환·에이전트 플랫폼 축으로 쓰인 글이다.
> 같은 판단이 이미 Bear Robotics 커버레터에 적혀 있다 — *「에이전트·하네스·MCP 서사를
> 붙이면 지원자가 무엇을 하는 사람인지 흐려진다」*. **첨부를 늘리는 것이 성의가 아니라,
> 그 자리에 맞는 것을 내는 것이 성의다.**
> → 임베디드 축의 깊이 문서가 필요하면 **별도 조준 빌드**가 답이다. `dossier/` 정본은 한 벌
> 뿐이라 조준부를 바꿔 재빌드해야 하고, 선례는 `../deepx--*/build.sh`(이력서 컷 +
> `experience-detail.md` → `pdfunite`) 다. **NEXT.md 에 남겼다.**

## 폼 실측 — 2026-08-01 (지원 URL 을 열어 DOM 으로 확인)

**예상이 틀렸다. 이 폼은 15개 항목이 전부 필수다.** 오른쪽 사이드바가 「전체 15개 항목」을
빨간 점으로 세고, DOM 의 `EditableLabel__RequiredMark` 가 15개 전부 렌더된다.
아래 「ninehire 는 서술형 칸이 거의 없다」는 **Enhans 한 건에서 일반화한 것이었고 여기서 깨졌다.**
ninehire 는 화이트라벨이라 **회사마다 폼을 따로 짠다 — 건마다 열어봐야 한다.**

| # | 항목 | 필수 | 무엇을 요구하는가 |
|---|---|---|---|
| 1–4 | 이름 · 생년월일 · 이메일 · 연락처 | ★ | 생년월일·연락처는 **Git 에 값이 없다 — GLG 직접** |
| 5 | 지원경로 | ★ | 드롭다운 (한화비전 선례는 **링크드인**) |
| 6 | 학력사항 | ★ | 구조화 입력 — `학력 구분` + 「입력사항 저장」 |
| 7 | 경력사항 | ★ | 구조화 입력 — 회사명 / 입사일 / 퇴사일 / 재직중 / 직급·직책 / 근무부서 / 고용형태.<br>**「일부 경력사항만 기재할 시 불이익이 있을 수 있다」**고 폼이 명시한다 |
| 8 | 경력요약 | ★ | **자유서술 10,000자.** 폼 예시는 서술문이 아니라 **용어 나열**이다 —<br>`ex) BSP Kernel Driver 개발 / Verilog HDL 능숙 / PCB Artwork` |
| 9 | 증명사진 | ★ | jpg·jpeg·png · 권장 250×300 px. ✅ **GLG 보유 확인 (2026-08-01)** |
| 10 | 이력서 | ★ | 「인적정보(생년월일/학력) + 경력기술서 + 자기소개서를 **포함한 하나의 파일**」.<br>⚠ **「연봉정보는 필히 제외」** — Embedded 컷은 연봉 미기재라 통과 |
| 11 | **포트폴리오(파일)** | ★ | **선택이 아니라 필수다.** 아래 §충돌 참조 |
| 12–14 | 장애여부 · 보훈여부 · 병역사항 | ★ | 전부 드롭다운. **GLG 직접 — Git 에 저장하지 않는다** |
| 15 | 영어 활용 수준 | ★ | 상/중/하 → `FAQ.md` §6 에 항목 추가함. **⚠ GLG 확정 필요** |

- **희망 연봉·입사 가능일 칸은 없다.** `FAQ.md` §4 를 쓸 자리가 이 폼엔 없다.
- 동의 2건 — **민감정보(필수)** · **인재풀(선택)**. 둘 다 「거부해도 제출 가능」이라고 적혀 있다.
- 하단에 **「지원서 미리보기」** 버튼이 있다. 제출 전에 여기서 한 번 본다.

### ✅ 해결됨 — 포트폴리오 필수 칸 (2026-08-09)

위 §「dossier 깊이 문서를 붙이지 않는다」는 **첨부가 선택 슬롯일 때의 판단**이었다.
이 폼은 포트폴리오를 **필수**로 받으므로 「안 붙인다」가 선택지가 아니었다. 그래서
**임베디드 축 깊이 문서를 따로 떴다** — `experience-detail.md` + `build.sh` + `pipeline/`,
산출 `submit/KimJunghan_Embedded_Experience_Detail.pdf` **3쪽**. AX Portfolio 12쪽을
그대로 내는 것은 지금도 이 자리의 답이 아니다.

**DEEPX 판을 그대로 복사하지 않았다.** 두 가지를 바꿨다.

1. **자가 측정 수치를 걷어냈다** (2026-07-29 GLG 원칙4). DEEPX 판 §3 에 부팅 45.4→5.8초 ·
   메모리 816→246MiB 가, §2 에 코드 공유 96% 가 살아 있다. 그때 나간 문서라 **그쪽은
   고치지 않지만**, 새로 나가는 문서는 원칙4 를 지킨다. 그 자리는 **진단·롤백 기준·성문화한
   승격 정책**으로 대체했다 — *「고친 것은 업스트림 릴리스였고 내 몫은 그것이 아니다」* 를
   문서가 직접 말한다.
2. **회사 중립판으로 썼다.** DEEPX 판은 `subtitle` 과 §0 에 회사명이 박혀 있어 재사용이
   막혔다. 이 판은 본문 어디에도 회사명이 없고, JD 대조표도 **BSP 드라이버 직무의 일반
   요건 9줄**로 세웠다. `build.sh` 의 **회사 중립 게이트**가 그것을 기계로 지킨다.

> 🔁 **그래서 이 문서는 이 건만의 것이 아니다.** 같은 파일이
> **Bear Robotics `기타 첨부 서류`(선택)** 와 **Telit Cinterion `Upload Additional Files`**
> 자리에 그대로 간다. 임베디드 축 세 건이 한 산출물을 나눠 쓴다.

## 폼에 답한 질문

| 질문 | 답 | 출처 |
|---|---|---|
| 이름 / 영문명 | 김정한 / Junghan Kim | `FAQ.md` §1 |
| 이메일 | [email removed] | `FAQ.md` §1 |
| 연락처 | **GLG 직접 입력** (문서에 값 없음) | `FAQ.md` §1 |
| GitHub / 포트폴리오 URL | https://github.com/junghan0611 | `FAQ.md` §1 |
| 총 경력 | **8+ years** — 요건 `3~15년` 통과 | `FAQ.md` §3 |
| 최종 학력 | 석사 보유 — 요건 `학사 이상` 통과 | `FAQ.md` §3 |
| 병역·보훈·장애 등 법적 값 | **브라우저에서 GLG 직접 입력. Git 에 저장하지 않는다** | `FAQ.md` §5 |
| 희망 연봉 | ✅ **「면접 후 협의」** — 숫자를 강제하는 폼에서만 GLG 가 직접 입력 (Git 에 숫자를 적지 않는다) | `FAQ.md` §4 |
| 입사 가능일 | ✅ **「협의 가능」** — 날짜를 강제하면 **「최종 합격·처우 협의 후 4주 이내」** | `FAQ.md` §4 |
| 이직 사유 | ✅ **확정 문장 있음** — `FAQ.md` §7 (`resume/body.org` Summary 축) | `FAQ.md` §7 |

> 사전에 없던 질문은 여기 적고 **`FAQ.md` 에도 추가한다.**
> ~~ninehire 계열은 **서술형 칸이 거의 없다**(Enhans 실측). 이력서 한 장이 사실상 전부일 수 있다.~~
> ⛔ **2026-08-01 실측으로 깨졌다.** 위 §폼 실측 참조 — 15개 항목 전부 필수이고
> **경력요약 10,000자 · 구조화 경력 · 증명사진 · 포트폴리오**를 받는다. 이 표는 폼의 절반도 못 덮는다.

## 왜 이 직무였는가

**Telechips 채용홈에 82건이 걸려 있지만 실제로 열려 있는 것은 3건뿐이다** — 그중 GLG 축에
걸리는 것은 이 하나다(나머지 둘은 `SoC Design Engineer(경력)` · `BD & Marketing Manager`).
「한 회사 한 직무」 고민이 필요 없는 건이다.

- **자격 4줄 중 3줄이 정면이다** — bootloader/커널 드라이버, Linux 커널 디버깅, 보드 bring-up.
  공고는 그중 **2개 이상**만 요구한다.
- **DEEPX Linux BSP 건과 같은 증거 패키지**를 쓴다. 그 건에서 Embedded 컷과 경력기술서를 이미
  한 번 조준해 냈다 — 이 건은 그 축의 재사용이다.
- **판교사옥**이라 통근 기준을 통과한다(경기 성남시 수정구 금토로80번길 27).
- 팀 소개가 「지식을 keep 하지 않고 share 하는 팀」을 반복해서 말한다. **문서·전파 축을 그대로
  꺼낼 수 있는 자리**다 — `AGENTS.md` §직무 선택 3번.

**경계는 먼저 밝힌다.** Hypervisor·Virtual platform·SOC Emulator·ASPICE·ISDB-T 는 없다(전부 우대
항목이다). 팹리스의 **칩 검증·불량 분석**은 인접이지 보유가 아니다 — 대는 것은 BSP·드라이버·
보드 bring-up 이다.

## 이후 기록

- [2026-07-28] GLG 지목으로 건 생성. LinkedIn `4431589221` 에서 본문 원문 채집.
- [2026-07-28] **지원 경로 확정 — 자체 채용홈 `careers.telechips.com`(ninehire 화이트라벨)**.
  채용홈 82건을 전부 훑어 `closedAt`/마감일로 걸러 **열린 3건**을 확인했다.
  `status=in_progress` · 상시 마감 · 정규직 · 경력 3~15년 · 판교사옥. 지원 URL HTTP 200 확인.
- [2026-07-28] 컷 **Embedded** 확정. 상태 `ready` — **GLG 노트북에서 제출 대기**.
- [2026-08-01] 제출 직전 `alive.py` 재확인 — **열림**(`status=in_progress` · `closedAt=None` · 상시).
  `submit/` 스냅샷 지문이 `resume/build` 원본과 동일함을 `sha256sum -c` 로 확인(`6a318434…`).
- [2026-08-01] **폼을 열어 15개 항목을 실측했다.** 위 §폼 실측. 아무 값도 입력하지 않고 닫았다.
- [2026-08-01] **⏸ 보류 — GLG 판단.** *「텔레칩스 미루기로 하자. 증명사진은 있는데
  임베디드로 지원하고 싶지 않다.」* 막힌 것은 증명사진도 포트폴리오도 아니고 **컷 자체**다.
  이 공고는 `Embedded S/W Engineer` 단일 직무라 **다른 컷으로 바꿔 낼 자리가 없다.**
  → 원장 상태는 `ready` 그대로 둔다(`LEDGER.md` §「상태와 「이번에 낼 것」은 다르다」).
  되살릴 조건은 **GLG 가 임베디드 축으로 낼 마음이 생기는 것** 하나다. 그때 포트폴리오
  필수 칸을 임베디드 조준 빌드로 채우고, 경력요약 10,000자를 쓴다.
- [2026-08-09] ⭐ **되살렸다 — GLG override.** 08-01 보류의 되살릴 조건이 그대로 충족됐다.
  위 보류 기록은 **지우지 않는다** — 그때의 판단도 사실이고, 바뀐 것은 GLG 의 결정이다
  (`AGENTS.md` §사람의 권한: *「앞선 권고를 없던 일로 고쳐 쓰지 말고 override 와 이유를 기록」*).
  같은 날 `AGENTS.md` §불변식이 이 건을 ⛔ 로 든 예시(*「기술 적합도는 높았지만 이 축으로는
  서지 않는다」*)도 남겨 둔다.
- [2026-08-09] **공고 생존 재확인** — `alive.py`: `status=in_progress · closedAt=None · 상시`.
- [2026-08-09] ✅ **막고 있던 포트폴리오 필수 칸을 열었다.** `experience-detail.md` ·
  `build.sh` · `pipeline/header.tex` 를 만들고 **3쪽** 산출. 위 §해결됨 절이 정본이다.
  게이트 셋을 빌드 안에 넣었다 — **활자 임베딩 · 원칙4 자가측정 수치 · 회사 중립**.
  `applications/check.py --deep` 도 통과했다(깊이 문서 28개 조준 검사).
- [2026-08-09] `stage.py` 의 `SLOT` 에 `_Experience_Detail` 항목을 추가했다. 없으면 세트
  README 의 칸이 `—` 로 비어 **「이 폴더가 올라갈 파일 전부다」가 어느 칸인지 말하지 못한다**
  — 08-01 IGNITE 에서 닫은 계약이 다시 열리는 자리였다.

### 2026-08-09 GLG 응답으로 닫힌 것

| # | 무엇 | 결정 | 어디에 있나 |
|---|---|---|---|
| 1 | **경력요약 10,000자** (폼 8번) | ✅ *「임베디드 관련 내 이력들로 적으면 되거든」* → **에이전트·AX 축을 한 줄도 넣지 않았다.** 폼 예시가 용어 나열형이라 나열을 골격으로 쓰되 맨 위에 두 줄 요약을 뒀다 | `answers.md` §문항 1 → `submit/answers.txt` (**1,976자** · 상한 10,000) |
| 2 | **영어 활용 수준** (폼 15번) | ✅ **「중」** | `FAQ.md` §6 에 확정으로 박았다 — 국문 3단 척도가 나오는 폼은 전부 이 값 |
| 3 | **증명사진** (폼 9번) | ✅ GLG 가 원본 지정 | 아래 §증명사진 |
| 4 | 생년월일·연락처·장애·보훈·병역 | **브라우저에서 GLG 직접** — Git 에 값을 두지 않는다 | 값의 SSOT 는 `../hyundai-autoever--ai-agent-engineer/submit/KimJunghan_AutoEver_Detail_Form.md` |

### 증명사진

- **원본**: `[private path removed][photo filename removed]` (960×960, 2023-10-21 · GLG 가 2026-08-09 지정).
  같은 폴더의 `[photo filename removed]` 는 **같은 사진의 흑백판**이다 — 별도 스튜디오 증명사진이 아니다.
- **크롭 명령** (재현 가능 · 산출물은 git 에 없다):

  ```bash
  magick '[private path removed][photo filename removed]' \
    -crop 480x576+90+0 +repage -resize 500x600! -quality 92 \
    submit/KimJunghan_ID_Photo.jpg
  ```

  5:6 비율(권장 250×300)의 2배로 뽑는다. 축소는 폼이 한다. `+90` 은 왼쪽 프레임에 걸린
  **다른 손님**을 잘라내기 위한 값이다.
- 🔴 **정직하게 적어 둔다 — 이것은 스튜디오 증명사진이 아니다.** 식당에서 찍은 사진이고,
  크롭해도 배경이 실내 간판이다. 한국 ATS 의 「증명사진」 칸에서 이것은 **첫 화면 신호**로
  읽힐 수 있다. **GLG 가 이 사진을 쓰기로 정했고 그대로 간다** — 다만 판단 근거를 남긴다.
  바꾸고 싶어지면 필요한 것은 정면·무배경 사진 한 장뿐이다.
- 🔴 **파일은 git 에 없다.** `.gitignore` 가 `applications/*/submit/*.jpg|jpeg|png` 를 막는다
  (2026-08-09 신설). 얼굴 이미지는 히스토리에 넣지 않는다 — PDF 와 같은 계약이고, 이유는
  `PUBLICATION.md` 의 공개 트리 계획이다. **다른 기계에서는 위 명령으로 다시 만든다.**
