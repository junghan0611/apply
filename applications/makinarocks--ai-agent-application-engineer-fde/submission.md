# 제출 기록 — MakinaRocks (마키나락스) / [FDE] AI Agent Application Engineer

| 항목 | 값 |
|---|---|
| 상태 | ✅ **submitted** — 2026-08-09 GLG 가 브라우저에서 최종 제출 완료 |
| 지원일 | **2026-08-09** |
| 경로 | **그리팅** (자체 도메인 `makinarocks.career.greetinghr.com`) — 가입 불필요 |
| 첨부 컷 | **FDE** (`resume/build/KimJunghan_Resume_Forward_Deployed_Engineer.pdf`, 3쪽) |
| 추가 첨부 | ✅ **슬롯 실측 완료 (2026-08-09)** — 전용 칸이 **둘** 있다: `경력기술서` → `dossier/build/KimJunghan_AX_Competency.pdf` (5쪽) · `포트폴리오` → `dossier/build/KimJunghan_AX_Portfolio.pdf` (12쪽) |
| 공고 URL | https://makinarocks.career.greetinghr.com/ko/o/214041 |
| **지원 URL** | **https://makinarocks.career.greetinghr.com/ko/o/214041** → 페이지 하단 「지원하기」 |
| 근무지 | 서울 서초구 강남대로 343 |
| 제품 | **DrawX** — 도면(PDF/DWG) B2B SaaS. Agent 아키텍처를 지금 설계하는 단계 |

## 낸 것 — ✅ 2026-08-09 실제로 올라간 것

**세 슬롯에 세 문서가 1:1 로 나갔다.** 스냅샷은 `submit/` 이고 지문은 `submit/MANIFEST.sha256` 이다.

- [x] **이력서** (필수 슬롯) — `submit/KimJunghan_Resume_Forward_Deployed_Engineer.pdf` · 33.3 kB
- [x] **경력기술서** (선택 슬롯) — `submit/KimJunghan_AX_Competency.pdf` (5쪽) · 330 kB
- [x] **포트폴리오** (선택 슬롯) — `submit/KimJunghan_AX_Portfolio.pdf` (12쪽) · 3.8 MB
- ⛔ 커버레터 — **폼에 서술형 칸이 없다**(실측). §왜 이 직무였는가 는 면접용으로 남는다.
- [x] 개인정보 **필수**항목 동의 · **선택**항목 동의 — GLG 가 체크.
      `제3자 이용제공`(선택)은 **켜지 않는 것을 권고**했다(`전체 동의` 대신 개별 체크).
- [x] 이름 · 이메일(+인증 코드) · 연락처 — **GLG 가 브라우저에서 직접 입력. 값은 저장하지 않는다.**

**⚠ Enhans·Reflection 과 같은 FDE 컷이다.** 업로드 파일명은 올리기 전에 확인했다.

> ✅ **07-30 GPT 교차검수 R3 가 이 건에서 닫혔다.** *「선택 첨부 관련 동의가 있는지 눈으로
> 확인한다 — 없다고 추론하지 않는다」* 는 경고였고, **있었을 뿐 아니라 첨부를 올리는 순간
> `(선택)` 이 `(필수)` 로 승격됐다.** §폼 스키마에 기록.

## 🔴 폼 스키마 — 브라우저 실측 (2026-08-09) · 그리팅이 신형 폼으로 바뀌었다

**07-28 에 `docsInfo` 로 읽은 「제출 서류는 Resume/CV 하나」는 더 이상 사실이 아니다.**
공고 `__NEXT_DATA__` 의 `openingsInfo` 에 **`useApplicationForm=true` + `applyConfigSnapshotKey`**
가 붙으면서 `docsInfo` · `questionnairesInfo` 가 **둘 다 `null`** 이 됐다. 스키마는 이제
지원 페이지(`/ko/o/214041/apply/new`, 클라이언트 렌더)에서만 읽힌다 —
**`boards.py` 로 폼을 안다고 가정하지 않는다.** (다른 그리팅 건에도 같은 변화가 왔을 수 있다.)

지원 URL: `https://makinarocks.career.greetinghr.com/ko/o/214041/apply/new` · **로그인 불필요**

| 구역 | 칸 | 필수 | 넣을 것 |
|---|---|---|---|
| 기본정보 | 이름 | ✅ | 김정한 |
| 기본정보 | 이메일주소 + **`이메일 확인` 버튼** | ✅ | [email removed] — 🔴 **인증 코드가 메일로 온다. GLG 가 직접 누르고 받는다** |
| 기본정보 | 연락처 (국가코드 `🇰🇷 +82` 고정) | ✅ | GLG 직접 입력 (Git 미보존) |
| 제출서류 | **이력서** | ✅ | `submit/KimJunghan_Resume_Forward_Deployed_Engineer.pdf` |
| 제출서류 | **경력기술서** | 선택 | `submit/KimJunghan_AX_Competency.pdf` (5쪽) |
| 제출서류 | **포트폴리오** | 선택 | `submit/KimJunghan_AX_Portfolio.pdf` (12쪽) |
| 동의 | 개인정보 **필수**항목 수집·이용 | ✅ | GLG |
| 동의 | 개인정보 **선택**항목 수집·이용 | 선택 | 🔴 **체크해야 선택 첨부가 처리된다** — 아래 |
| 동의 | 개인정보 **제3자** 이용제공 | 선택 | GLG 판단 |

- ⭐ **서류 슬롯이 세 개고, 우리가 준비한 세 문서와 정확히 1:1 이다.** 이력서 한 장으로 끝나지 않는다.
- 🔴🔴 **선택 첨부를 올리면 「개인정보 선택항목 수집 및 이용 동의」가 `(선택)` → `(필수)` 로
  바뀐다** (2026-08-09 실측 · 파일 3개 업로드 직후 라벨이 실제로 승격되는 것을 확인).
  07-30 GPT 교차검수 R3 이 *「미동의로 내면 올린 첨부가 처리되지 않는다」* 고 경고한 것의
  **정확한 기계적 원인**이다 — 폼이 아예 제출을 막는다. 선택 동의문이 수집 대상에
  *「이력서, 자격사항, 경력사항, 포트폴리오, 경력기술서…」* 를 명시하기 때문이다.
  ⭐ **그래서 「첨부를 더 내는 결정」과 「동의를 더 켜는 결정」은 같은 결정이다.** 분리되지 않는다.
- **`전체 동의` 를 쓰지 않는다.** 그것을 켜면 `개인정보 제3자 이용제공 동의`(선택, 필수 아님)까지
  함께 켜진다. **필수 2개만 개별로 체크한다.**
- ✅ **그리팅은 에이전트 파일 업로드가 된다** (2026-08-09 실측). ninehire(Enhans·한화비전)에서
  막혔던 것과 다르고 `recruiter.co.kr` 과 같다. 3개 슬롯에 저장소 경로를 그대로 올렸다
  (33.3kB · 330kB · 3.8MB). **`applications/AGENTS.md` §외부 ATS 에 올릴 값이다.**
- ⛔ **자기소개서·서술형 칸이 없다.** 커버레터를 붙일 자리도 없다 — §왜 이 직무였는가 는 면접용으로 남는다.
- 파일 input 세 개에 `accept` 제한이 없다(PDF 그대로 올라간다).

## 폼에 답한 질문

| 질문 | 답 | 출처 |
|---|---|---|
| 이름 / 영문명 | 김정한 / Junghan Kim | `FAQ.md` §1 |
| 이메일 | [email removed] | `FAQ.md` §1 |
| GitHub / 포트폴리오 URL | https://github.com/junghan0611 · https://notes.junghanacs.com | `FAQ.md` §1 |
| 총 경력 | **8+ years** — 요건 `LLM/VLM 프로젝트 1년 이상` 은 여유 있게 통과 | `FAQ.md` §3 |
| 최종 학력 | 석사 보유 | `FAQ.md` §3 |
| 연락처 · 법적 값 | **브라우저에서 GLG 직접 입력. Git 에 저장하지 않는다** | `FAQ.md` §5 |
| 희망 연봉 | ✅ **「면접 후 협의」** — 숫자를 강제하는 폼에서만 GLG 가 직접 입력 (Git 에 숫자를 적지 않는다) | `FAQ.md` §4 |
| 입사 가능일 | ✅ **「협의 가능」** — 날짜를 강제하면 **「최종 합격·처우 협의 후 4주 이내」** | `FAQ.md` §4 |
| 이직 사유 | ✅ **확정 문장 있음** — `FAQ.md` §7 (`resume/body.org` Summary 축) | `FAQ.md` §7 |

> 그리팅은 회사마다 폼이 다르다. 이 건은 `docsInfo` 가 **Resume/CV 하나**였다.
> 전형: **서류검토 → 사전인터뷰(온라인) → 기술인터뷰 → Fit인터뷰**. 코딩테스트는 없다.

## 왜 이 직무였는가

**마키나락스는 FDE 만 12건이 열려 있다.** 국내에서 FDE 축이 가장 두꺼운 회사이고, 그중
이 자리를 고른 이유는 **요구 문장 세 줄이 GLG 가 만든 것과 그대로 겹치기 때문**이다.

- **`Agentic 검색 — 쿼리 생성부터 결과 판단, 재검색까지 Agent 가 자율 수행`** ·
  `Vector DB · Hybrid Search` — 하이브리드 검색(벡터+FTS) 색인을 **직접 만들어 매일 운영한다.**
- **`Agent 가 공유하는 도구·메모리·판단 체인을 추상화`** — 하나의 ID 축으로 말이 통하는
  CLI 한 벌과, 기억을 API 로 추상화한 축이 그 자리에 정확히 붙는다.
- **`Agent 아키텍처의 근본 설계를 지금 함께 결정한다`** — 완성된 시스템에 기능을 얹는 자리가
  아니다. 「플레이북이 아직 없는 곳에서 구조를 세우는 일」이 GLG 가 반복해 온 방식이다.
- **FDE 본부의 정의가 정면이다** — 「모델을 만드는 것이 아니라 현장의 문제를 구조화해
  자율 실행 Agent 로 구현해 제품에 탑재」. 고객 현장 축(NEMO-UX 4년·월패드 납품)과 이어진다.

**경계는 먼저 밝힌다.** **CAD/도면 데이터 경험이 없다**(우대 항목). **VLM(비전) 실적이 얇다** —
텍스트·검색 축이 두껍다. `LangGraph 등 프레임워크`는 **쓴 쪽이 아니라 직접 만든 쪽**이라
「사용 경력」으로 포장하지 않는다.

## 이후 기록

- [2026-07-28] 건 생성. **`boards.py` 로 그리팅 본문을 브라우저 없이 채집** — 본문뿐 아니라
  제출 서류(`Resume/CV`)와 서술형 문항까지 폼을 열기 전에 확인했다.
- [2026-07-28] 컷 **FDE** 확정. 상태 `ready` — **GLG 노트북에서 제출 대기**.
- [2026-08-09] `alive.py` → **OPEN** (greeting `status=OPEN` · 마감일 없음). 브라우저로 신형
  지원폼을 실측해 §폼 스키마를 채웠다 — **서류 슬롯 3개 · 선택 동의 필수 조건 · 이메일 인증 게이트**
  가 새로 드러났다. 첨부 판정을 「미실측」에서 **확정**으로 바꿨다.
- [2026-08-09] ✅ **제출 완료.** 에이전트가 세 슬롯에 파일 3개를 올리고(그리팅은 에이전트
  업로드가 된다 — 신규 실측), **이름·이메일 인증·연락처·동의·최종 「제출하기」는 GLG 가** 했다.
  완료 화면 확인: `…/o/214041/finish?submittedEmail=…` · *「지원서를 성공적으로 제출했습니다.
  작성하신 이메일 주소([email removed])로 곧 지원 확인 메일이 발송될 예정」*.
  **개인 필드 값은 이 저장소에 저장하지 않았다.**
  ⭐ **이 건이 남긴 것 둘** — ① 첨부와 동의는 분리된 결정이 아니다(선택 첨부 → 선택 동의가
  필수로 승격) ② 그리팅은 에이전트 파일 업로드가 가능하다(ninehire 와 다르다).
