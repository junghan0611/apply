# 제출 기록 — ㈜딥엑스 / [SW] Linux BSP Engineer

| 항목 | 값 |
|---|---|
| 상태 | **submitted** |
| 지원일 | 2026-07-28 |
| 마감 | 없음 (채용 시 마감) |
| 경로 | 그리팅(greetinghr) 자체 도메인 — 폼 구조는 `166870` 과 동일 |
| 첨부 컷 | **Embedded Software Engineer** |
| 근무지 | 경기 성남 판교 미래에셋벤처타워 — 기준 적합 |
| 공고 URL | https://deepx.career.greetinghr.com/ko/o/146732 |

## 낸 것

- [x] **1. 입사지원서 (필수)** — `submit/KimJunghan_DEEPX_BSP_Application.pdf`
      **5쪽 = 이력서 2쪽(Embedded 컷) + 경력기술서 3쪽**
- [ ] 2. 성적증명서 — 제출 대상 아님(3년 미만만 필수)
- [x] 3. 포트폴리오 — URL `https://github.com/junghan0611`
- [ ] 4. 기타 증빙자료 — 없음
- [x] 기본정보·추가질문·법적 필드 — GLG가 브라우저에서 사실값으로 완료, 민감값 미보존
- [x] 3.1 사유 (300자) — `form-answers.md`의 제출 본문
- [x] 필수 동의·최종 제출 — GLG가 직접 완료

```bash
./build.sh && ./count.sh
```

## 제출 순서 — 에이전트 권고와 GLG의 실제 결정

에이전트들은 처음에 `166870`을 먼저 내고 3~5일 뒤 재판단하자고 권고했다. 근거는 아래와
같았다. GLG는 두 증거 패키지를 확인한 뒤 **같은 날 둘 다 제출**하기로 명시적으로 결정했다.
이 절은 실패한 권고를 지우지 않고 사람의 override가 어디서 일어났는지 보존한다.

- **딥엑스는 CEO 면접까지 있는 촘촘한 전형이다.** 지원 이력이 내부에서 같이 읽힌다.
- **성격이 정반대인 두 직군(에이전트 인프라 / 리눅스 BSP)에 같은 날 지원하면**
  「스펙트럼이 넓다」가 아니라 **「아무 데나 넣는다」**로 읽힌다.
- **이길 수 있는 자리는 `166870` 이다.** 거기 집중한 인상을 먼저 만든다.
- 이 공고는 **마감이 없다.** 서두를 이유가 없다.

**실제 결정 — 2026-07-28 같은 날 제출.** 기본 원칙보다 GLG의 명시 판단이 우선했다.

## 이 건의 정직한 값어치

`JD.md` §판정 표에 다 적었지만 한 줄로 줄이면 이렇다.

> **필수 자격 여섯 중 둘(U-Boot·디바이스 트리)이 완전한 공백이고, 하나(Assembly)가 없다.**
> 대신 **제품을 실리콘에서 양산까지 보낸 세로축**과 **ARM→RISC-V 크로스아키텍처 포팅 +
> board HAL 추출**이 있다.

그래서 이 지원서는 **경계를 세 곳에서 먼저 말한다** — 경력기술서 §0(첫 쪽), §5(마지막 쪽),
그리고 300자 사유. 감추고 통과하면 기술 면접 첫 10분에 드러나고 그때는 신뢰까지 잃는다.
공고 §Notes 가 허위 기재 시 합격 취소를 명시한 회사이기도 하다.

**이 방식이 지는 지원을 이기게 만들지는 않는다.** 다만 「BSP 순수 전문가는 아니지만 보드
경계를 아는 제품 엔지니어가 필요한가?」라는 질문을 팀에게 정확히 던진다. 그 질문에 「그렇다」인
팀이면 붙고, 아니면 서로 시간을 아낀다.

## 이 자리에서만 통하는 카드 하나

**딥엑스는 NPU 반도체 회사이고, GLG는 IITP 국책 과제(2025–2028)에서 NPU 워크로드를
가속기 간에 포팅하는 중이다.** 그리고 RPi5 + Hailo-8 을 Yocto 로 배포했다.

이 사실은 `166870`(사내 개발 생산성)에서는 곁가지라 일부러 눌렀지만,
**여기서는 「우리 칩에 리눅스를 올릴 사람」이라는 자리와 정면으로 만난다.**
경력기술서 §0 이 그 점을 지원 동기로 쓰고 있다.

## 제출 전 판단 기록 — 완료된 의사결정

### ① 제출 여부와 개인 필드

GLG가 제출을 선택했고, 필수 개인·법적 필드는 브라우저에서 직접 사실값으로 입력했다.
민감값은 Git에 저장하지 않는다.

### ③ 세 번째 공고 `114813 System SW` 는 자리를 만들지 않았다

1번의 ★★ 평가에 동의한다. ROM boot code · PCIe/DDR/MIPI/ISP 드라이버 · PMU/MMU ·
JTAG/ETM/SWD 축이라 **BSP 건보다 더 멀다.** 경력기술서 §5 가 그 사실을 명시적으로 적어
두었다 — *"I know this is where the neighbouring System SW posting lives, and I am not applying
to it for that reason."* 두 자리에 다 내지 않는다는 것을 서류에서 스스로 밝히는 편이 낫다.

## 이후 기록

- [2026-07-28] 건 생성. 1번이 딥엑스 세 공고를 읽고 ★★★으로 2순위 판정.
- [2026-07-28] 공고 원문·폼 스키마 채집. 폼이 `166870` 과 동일함을 확인.
- [2026-07-28] 경력기술서 3쪽 작성. **§0 첫 쪽에서 U-Boot·디바이스 트리·커널 드라이버·
  Assembly 공백을 먼저 밝히는 구조**로 썼다. 이력서(Embedded 컷)와 통합해 5쪽.
- [2026-07-28] GLG가 개인 필드·추가질문·동의·최종 제출을 직접 완료. **제출 완료.**
