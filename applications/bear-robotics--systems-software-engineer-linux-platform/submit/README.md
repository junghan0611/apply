# 제출 세트 — bear-robotics--systems-software-engineer-linux-platform

**이 폴더가 실제로 올라갈 파일 전부다.** 검수는 여기서 한다 — 소스(org/md)가 아니라
나갈 물건 위에서. 고칠 것이 있으면 org·파이프라인을 고치고 `./stage.py` 를 다시 돌린다.

🔴 **2026-08-09 제출 완료 — 이 표는 「올라갈 것」이 아니라 「무엇이 실제로 나갔나」다.**
`submitted` 건은 `stage.py` 가 건드리지 않으므로 **손으로 표시했다**(그 이유는
`../submission.md` §낸 것). 여기부터는 재생성하지 않는다.

| 파일 | 폼의 어느 칸 | 실제로 나갔나 |
|---|---|---|
| `KimJunghan_Resume_Embedded_Software_Engineer.pdf` | 이력서 (필수) | ✅ **나갔다** — 폼에 올린 유일한 파일 |
| `cover-letter.txt` | 커버레터 textarea — 전체 복사해 붙여넣었다 (2,762자) | ✅ **나갔다**(파일이 아니라 본문으로) |
| `KimJunghan_Embedded_Experience_Detail.pdf` | `기타 첨부 서류`(선택) — 임베디드 축 깊이 문서 3쪽 | ⛔ **안 나갔다.** GLG 판단으로 이력서 한 파일만 냈다. 파일은 다음 임베디드 건을 위해 세트에 남겨 둔다 |

생성: `applications/stage.py` · 손으로 고치지 않는다.
