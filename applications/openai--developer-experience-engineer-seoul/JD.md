# JD source revision 10

The raw job-posting text that lived here was removed at publication.
Only the path and the fact that a new revision landed in this commit are kept;
the ordinal is an opaque counter and cannot be reversed into the original text.

Why, and what else was removed: see docs/HISTORY-REDACTION.md
