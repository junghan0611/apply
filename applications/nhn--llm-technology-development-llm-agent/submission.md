# 제출 기록 — NHN / LLM 기술 개발 (LLM / Agent)

| 항목 | 값 |
|---|---|
| 상태 | **draft** — 첨부는 다 있다. **지원 URL 하나가 비었다** |
| 지원일 | — |
| 경로 | **NHN 자체 채용홈** — URL 미확정 |
| 첨부 컷 | **AI Engineer** + **포트폴리오(필수)** — `../../nhn/` 산출물 재사용 |
| 공고 URL | https://www.linkedin.com/jobs/view/4439978335/ |
| **지원 URL** | ⛔ **미확인.** 2026-07-20 첫 지원 때의 경로를 쓰거나, LinkedIn 「회사 웹사이트에서 지원」 확인 |
| 근무지 | **판교 삼평동 플레이뮤지엄** · 정규직 |
| 자격 | **석사 이상 또는 경력 2년 이상** + LLM 기반 지식 |

> **NHN 재지원.** 2026-07-20 `AX Transformation / AI Infra (BE)` 제출 완료(`../../nhn/`).
> **다른 포지션**이고, 그때 만든 포트폴리오가 이 건의 필수 서류를 그대로 덮는다.

## 왜 이 건인가 — GLG 판단 (2026-07-28)

> "nhn에 추가로 이것도 지원해야겠다."

첫 지원이 **AI 인프라 백엔드** 축이었다면 이 건은 **LLM/Agent 기술 개발** 축이다. 같은 회사에
성격이 다른 두 자리를 내는 것이고, **두 번째 자리가 GLG 의 실제 작업물과 더 가깝다.**

주요 업무 다섯 줄 중 세 줄이 지금 하는 일 그대로다:

- `Agent Orchestration 설계 및 자동화 Tool 개발` → `entwurf` · `agent-config` 40+ 스킬 · n8n 40+ 노드
- `사내 부서 Agent 개발 요청에 대한 기술 컨설팅 및 구성 지원` → **현직에서 지금 하는 일.**
  다른 팀이 요청하는 것은 대시보드가 아니라 "**나에게 의존하지 않고 돌아가게 해 달라**"이고,
  그때마다 도메인 오너에게 에이전트를 붙여 왔다
- `RAG 파이프라인 구축·운영 (청킹/임베딩, 벡터 DB 색인·검색 품질 튜닝)` → pgvector 2,945 임베딩 +
  임베딩·리랭킹 **직접 서빙** · `andenken` 하이브리드 검색(vector+FTS 점수 정규화)

## 첨부 — 이미 만들어져 있다

공고가 **포트폴리오 제출을 필수**로 못 박았다(`*서류 제출 시, 포트폴리오 제출 필수`).
`../../nhn/` 에 그 물건이 이미 있다:

| 산출물 | 소스 |
|---|---|
| 역량·성과 기술서 PDF | `nhn/competency.org` |
| 포트폴리오 PDF | `nhn/portfolio.org` |
| 상세 증거 Markdown | `nhn/detail.org` |
| 참고문헌(공개 URL·commit·PR·npm·라이브 통계) | `nhn/references.bib` |

**이 건에 맞게 재편해서 `submit/` 에 사본을 둔다.** `nhn/` 원본은 그 건의 증언이므로 건드리지
않는다 — 재빌드는 `nhn/run.sh` 로 하고 결과만 가져온다.

## 절차 — 다른 건들과 다르다

**서류전형 → 라이브코딩 → T인터뷰(Tech) → C인터뷰(Career) → 처우협의 → 입사**

- **라이브코딩이 서류 바로 다음이다.** 합격 시 **사전 과제**까지 있다.
- 우대에 `Python 개발이 능숙하신 분` 이 있고 업무도 Python 계열이다. GLG 주력은
  Go·Clojure·Zig·TS 이므로 **Python 라이브코딩 대비가 이 건의 실질 관문**이다.

## 경계 — 정직하게

- **도메인 특화 모델 학습(LoRA·Scratch fine-tuning) 경험 없음.** 주요 업무에 `모델 학습 및
  최적화 관련 업무` 가 있다. 서빙·배포(NPU 이식, sLLM 음성 제어)는 있지만 **학습은 아니다.**
- **AI/NLP 논문·챌린지 입상 없음.** 대학원 축은 시스템(NVM 파일시스템·NUMA)이다.
- 병역·해외여행 결격 사유 조항 있음 — 해당 없음으로 안다(확인은 폼에서).

## 낸 것

- [ ] **이력서 PDF** — `resume/build/KimJunghan_Resume_AI_Engineer.pdf`
- [ ] **포트폴리오 (필수)** — `../../nhn/` 산출물 재편 → `submit/`
- [ ] 자기소개 — 공고가 "직무 역량을 경험과 잘 연결지어" 요청. 프로젝트 단위로 정리

## 폼에 답한 질문

| 질문 | 답 | 출처 |
|---|---|---|
| (지원 URL 확정 후 실측) | | |

## 이후 기록

- [2026-07-28] 건 생성. GLG 가 링크를 던졌다. LinkedIn guest API 로 **본문 전문 확보**.
  **NHN 두 번째 건**이고 첫 건(`../../nhn/`)의 포트폴리오가 이 건의 필수 서류를 덮는다.
