# 제출 기록 — NHN / LLM 기술 개발 (LLM / Agent)

| 항목 | 값 |
|---|---|
| 상태 | **draft** — 지원 URL 확정. **폼은 로그인 뒤에 있다** |
| 지원일 | — |
| 경로 | **NHN 자체 채용홈** `careers.nhn.com` — 확정 |
| 첨부 컷 | **AI Engineer** + **포트폴리오(필수)** — `../../dossier/` 산출물 재사용 |
| 공고 URL | https://www.linkedin.com/jobs/view/4439978335/ |
| **지원 URL** | **`https://careers.nhn.com/recruits/4370711607830110861`** ✅ |
| 공고 제목(원문) | **`[NHN] LLM 기술 개발 (LLM / Agent)`** · `Tech | AI/ML | 경력 | 정규 | 채용시까지` |
| 근무지 | **판교 삼평동 플레이뮤지엄** · 정규직 |
| 자격 | **석사 이상 또는 경력 2년 이상** + LLM 기반 지식 |

> **NHN 재지원.** 2026-07-20 `AX Transformation / AI Infra (BE)` 제출 완료(`../../dossier/`).
> **다른 포지션**이고, 그때 만든 포트폴리오가 이 건의 필수 서류를 그대로 덮는다.

## 왜 이 건인가 — GLG 판단 (2026-07-28)

> "nhn에 추가로 이것도 지원해야겠다."

첫 지원이 **AI 인프라 백엔드** 축이었다면 이 건은 **LLM/Agent 기술 개발** 축이다. 같은 회사에
성격이 다른 두 자리를 내는 것이고, **두 번째 자리가 GLG 의 실제 작업물과 더 가깝다.**

주요 업무 다섯 줄 중 세 줄이 지금 하는 일 그대로다:

- `Agent Orchestration 설계 및 자동화 Tool 개발` → `entwurf` · `agent-config` 40+ 스킬 · n8n 40+ 노드
- `사내 부서 Agent 개발 요청에 대한 기술 컨설팅 및 구성 지원` → **현직에서 지금 하는 일.**
  다른 팀이 요청하는 것은 대시보드가 아니라 "**나에게 의존하지 않고 돌아가게 해 달라**"이고,
  그때마다 도메인 오너에게 에이전트를 붙여 왔다
- `RAG 파이프라인 구축·운영 (청킹/임베딩, 벡터 DB 색인·검색 품질 튜닝)` → pgvector 2,945 임베딩 +
  임베딩·리랭킹 **직접 서빙** · `andenken` 하이브리드 검색(vector+FTS 점수 정규화)

## 첨부 — 이미 만들어져 있다

공고가 **포트폴리오 제출을 필수**로 못 박았다(`*서류 제출 시, 포트폴리오 제출 필수`).
`../../dossier/` 에 그 물건이 이미 있다:

| 산출물 | 소스 |
|---|---|
| 역량·성과 기술서 PDF | `dossier/competency.org` |
| 포트폴리오 PDF | `dossier/portfolio.org` |
| 상세 증거 Markdown | `dossier/detail.org` |
| 참고문헌(공개 URL·commit·PR·npm·라이브 통계) | `dossier/references.bib` |

**이 건에 맞게 재편해서 `submit/` 에 사본을 둔다.** `dossier/` 원본은 그 건의 증언이므로 건드리지
않는다 — 재빌드는 `dossier/run.sh` 로 하고 결과만 가져온다.

## 절차 — 다른 건들과 다르다

**서류전형 → 라이브코딩 → T인터뷰(Tech) → C인터뷰(Career) → 처우협의 → 입사**

- **라이브코딩이 서류 바로 다음이다.** 합격 시 **사전 과제**까지 있다.
- 우대에 `Python 개발이 능숙하신 분` 이 있고 업무도 Python 계열이다. GLG 주력은
  Go·Clojure·Zig·TS 이므로 **Python 라이브코딩 대비가 이 건의 실질 관문**이다.

## 경계 — 정직하게

- **도메인 특화 모델 학습(LoRA·Scratch fine-tuning) 경험 없음.** 주요 업무에 `모델 학습 및
  최적화 관련 업무` 가 있다. 서빙·배포(NPU 이식, sLLM 음성 제어)는 있지만 **학습은 아니다.**
- **AI/NLP 논문·챌린지 입상 없음.** 대학원 축은 시스템(NVM 파일시스템·NUMA)이다.
- 병역·해외여행 결격 사유 조항 있음 — 해당 없음으로 안다(확인은 폼에서).

## 낸 것

- [ ] **이력서 PDF** — `resume/build/KimJunghan_Resume_AI_Engineer.pdf`
- [ ] **포트폴리오 (필수)** — `../../dossier/` 산출물 재편 → `submit/`
- [ ] 자기소개 — 공고가 "직무 역량을 경험과 잘 연결지어" 요청. 프로젝트 단위로 정리

## 지원 경로 — 브라우저 실측 (2026-07-29)

**`https://careers.nhn.com/recruits/4370711607830110861`** — 공고 살아 있음(`채용시까지`).
LinkedIn 「회사 사이트에서 지원」이 이 URL 로 튄다.

**⛔ 폼은 로그인 뒤에 있다.** `careers.nhn.com` 우상단이 `로그인` 이고, 이 브라우저 세션은
비로그인 상태다. **에이전트는 로그인하지 않는다** — GLG 가 **2026-07-20 첫 지원 때 만든 계정**
으로 들어간다.

**대신 이 건이 가벼워지는 이유가 하나 있다.** 사이트 상단에 **「기본 지원서」** 메뉴가 있다.
NHN 채용홈은 지원서를 계정에 저장해 두고 공고마다 재사용하는 구조다 — **2026-07-20 에 채운
인적사항·학력·경력이 이미 그 계정에 있다.** 이 건에서 새로 할 일은
**직무 맞춤 자기소개 + 포트폴리오 교체**로 좁혀진다.

> ⚠ **기본 지원서를 덮어쓰기 전에 확인한다.** 첫 지원(`AX Transformation / AI Infra`)의
> 제출본이 그 계정에 남아 있다. 이 건에 맞춰 자기소개를 고칠 때 **첫 건의 제출 내용이
> 사후 변경되지 않는지** 화면에서 확인한다. NHN 은 같은 회사 두 번째 건이라 이 위험이 있다.

**공고가 지원서 작성법을 직접 지시했다** — 그대로 따른다.

- *"해오신 업무를 프로젝트 단위로 잘 정리해서 적어주세요"* → 이력서가 이미 프로젝트 단위다
- *"포트폴리오, 노션, 깃허브 등 … 함께 제출해주세요"* · **`*서류 제출 시, 포트폴리오 제출 필수`**
- *"자기소개는 직무 역량을 경험과 잘 연결지어"*
- *"학력/경력은 빠짐없이 모두 작성"* → **공백기(2022.06–2025.05)를 빼지 않는다.**
  `FAQ.md` §3 의 공백기 화법을 그대로 쓴다

## 폼에 답한 질문

| 질문 | 답 | 출처 |
|---|---|---|
| (로그인 뒤 — GLG 가 브라우저에서 실측) | | |

## 이후 기록

- [2026-07-28] 건 생성. GLG 가 링크를 던졌다. LinkedIn guest API 로 **본문 전문 확보**.
  **NHN 두 번째 건**이고 첫 건(`../../dossier/`)의 포트폴리오가 이 건의 필수 서류를 덮는다.
- [2026-07-29] **브라우저로 지원 URL 확정**(`careers.nhn.com/recruits/4370711607830110861`).
  공고 본문을 채용홈 원본에서 다시 대조했고 LinkedIn 판과 일치한다. **폼은 로그인 뒤**라
  스키마를 못 떴다 — GLG 계정 몫. **「기본 지원서」 재사용 구조**를 발견해 기록했다.
