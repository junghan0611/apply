# AGENTS.md — apply (공개 채용 지원 파이프라인 사례)

`apply` 는 **증거 우선 · 사람이 최종 결정하는** 채용 지원 워크스페이스이자 공개 사례 기록이다.
탐색 · 문서 생성 · 브라우저 제출 · 메타 검수를 레인으로 갈라, 병렬로 도는 에이전트가 서로의
SSOT 를 덮지 않게 한다.

GitHub `junghan0611/apply`는 공개 저장소다. 회사·직무·지원 답변·합격/불합격의 판단 계보는
숨기지 않지만, 개인정보·제3자 원문·개인화 ATS 경로·현직 비밀·실제 제출 바이너리는 담지 않는다.
정확한 공개 경계와 history 정제 방식은 `PUBLICATION.md`, 라이선스는 `LICENSING.md`가 정본이다.
raw 폼과 법적 값은 private Org가 소유하며 이 저장소에 사본을 만들지 않는다.

## 담당자 문서 — 제목이 아니라 denote id 로 기억한다

| 무엇 | denote id | 사는 곳 |
|---|---|---|
| **담당자 문서** (이 리포의 공개 얼굴) | `20260331T172313` | `~/org/botlog/` |
| **곁노트** — 이 리포의 「왜」 층 (autholog) | `20230725T102900` | `~/org/notes/` |

- **제목으로 찾지 않는다.** 제목·슬러그·태그·파일명은 하루에도 바뀌고 `#+identifier` 만 안 움직인다.
  2026-09-04 에 이 노트가 개명되며 넷이 전부 바뀌었고, 제목으로 유도한 순회는 그 사이 `후보 없음`
  이라 답했다. **어느 노트가 담당자 문서인가는 GLG 의 판정이지 유도할 사실이 아니다.**
- **읽는 자리는 `~/org/botlog/` 의 org 원본이다.** `notes/content/` 로 내보내진 md 는 한 주기 늦으므로
  판정 근거로 쓰지 않는다.
- **곁노트를 담당자 문서에 흡수하지 않는다.** 「왜」(원석)와 「어떻게」(담당자 문서)는 이웃으로 둔다.
- 읽기는 `denotecli read <id> --outline` 으로 뼈대부터. 쓰기는 `botlog` 스킬
  (`agent-denote-add-history` / `agent-denote-add-heading`). 내용을 실제로 고쳤으면
  `agent-denote-set-front-matter` 로 `:hugo_lastmod` 도장을 찍는다 — 히스토리 한 줄은 로그이지 수정이 아니다.

## 여기부터 읽는다

1. `NEXT.md` — 지금 할 한 걸음.
2. 손댈 레인의 `AGENTS.md` — 그 레인의 계약.
3. `git status --short` — 옆 파일을 다른 에이전트가 쥐고 있을 수 있다.
4. `edit`/`write` 전에 기존 파일을 읽는다. 기억으로 파일을 덮는 것이 동시 작업을 날리는 경로다.
5. **커밋 · 푸시는 GLG 가 이번 세션에서 명시로 요청할 때만 한다.**

### 커밋·푸시와 활동 타임스탬프

이 저장소 자체가 지원 회사·전형 결과의 공개 시간축이다. **커밋·푸시 후 agenda 활동 타임스탬프는
별도로 찍지 않는다.** 같은 사건을 다른 공개 타임라인에 중복 기록하지 않는다는 뜻이며,
이 저장소의 commit history와 `applications/LEDGER.md`가 활동 기록을 소유한다.

### 새 세션은 문서 생성부터 시작하지 않는다

이 저장소는 이력서 생성기가 아니라 **채용 지원용 증거 컴파일러**다. 새 세션은 먼저 현재 판을
복원하고, 이미 있는 정보를 더 조이는 데서 시작한다. 아래 질문에 답하지 못한 채 새 파일이나 새
서사를 만들지 않는다.

| 먼저 답할 질문 | 읽을 곳 |
|---|---|
| 지금 사람이 해야 할 한 걸음은 무엇인가 | `NEXT.md` §NOW |
| 이미 냈거나 준비 중인 건은 무엇인가 | `applications/LEDGER.md` |
| 이 건의 공개 원문 요약 · 판단 · 답변 · 첨부 지문은 무엇인가 | 해당 `JD.md` · `submission.md` · `submit/MANIFEST.sha256` |
| 아직 케이스가 아닌 후보와 출처 흔적은 무엇인가 | `applications/INBOX.md` |
| 반복 폼 값과 아직 사람 결정이 필요한 값은 무엇인가 | `applications/FAQ.md` |
| 이력서 문장의 사실 · 수치 · 시간 계보는 무엇인가 | `MATERIAL.md` (프로필 repo `junghan0611/junghan0611`의 `apply/` — **이 저장소가 아니다**) · `resume/body.org` |
| 직무별로 무엇을 선택하고 어떤 순서로 보이는가 | `resume/targets/*.org` |
| 공고를 어떻게 찾고 원문을 어떻게 확인하는가 | `.claude/skills/linkedin-jobs/SKILL.md` |

이미 케이스가 있으면 그 파일을 이어서 편집한다. 같은 회사·직무의 폴더, 별도 메모, 새 사실 사본을
만드는 것은 진전이 아니라 **새로운 불일치 지점**을 만드는 일이다.

## 이 저장소가 조이는 흐름

```text
공고 흔적
  → 회사 공식 보드와 원본 ATS 확인
  → JD 원문·출처·미확인값 보존
  → 필수요건과 검증 사실 대조
  → 한 회사에서 낼 직무와 이력서 컷 선택
  → 공개 폼·제출서류·질문 확인
  → 부족한 정보는 정확한 요청으로 남김
  → 제출 패킷을 ready 로 만듦
  → GLG 가 노트북 브라우저에서 검토·개인값 입력·업로드·Submit
  → private 원본과 public redacted 원장·지문을 submitted 로 함께 닫음
```

탐색 채널의 위계는 **회사 공식 채용 보드/ATS → LinkedIn 정식 공고 → LinkedIn 개인 채용 포스트
→ Wanted·Remember 등 폐쇄 채널의 흔적 → 검색 캐시·팀 글**이다. 뒤쪽 채널은 후보를 발견하는
신호일 수 있지만, 앞쪽 원본과 현재 열림 여부를 확인하기 전에는 확정 JD가 아니다. LinkedIn 추천
목록을 시장 전체로 취급하지 않는다.

## 레인과 소유권

| 레인 | 위치 | 소유 | 소유하지 않음 |
|---|---|---|---|
| 탐색 | `.claude/skills/linkedin-jobs/` | 공개 카드 · 포스트, 원문 요건, 근무지 확신도, 랭킹 근거, 추천 컷 | 로그인, 개인 필드, 제출 |
| 문서 | `resume/` | 검증된 사실 모듈, 직무별 선택 · 순서, 공개 증거 입구, 재현 가능한 PDF | ATS 상태, 지원 원장 |
| 제출 | `applications/` | JD 요약·출처, 공개 가능한 답변, 업로드 지문, 상태 원장 | raw 폼·법적 값·실제 업로드 바이너리 |
| **dossier 발사대** | `dossier/` | 역량기술서 · 포트폴리오 · 증거패키지 생성, 면접 앵커 | 현재 지원 큐 |
| 메타 검수 | 루트 문서 · `scripts/` | 계약, 모순 검사, 핸드오프 | 남의 레인 구현을 말없이 다시 여는 것 |

한 에이전트가 "도와준다"며 다른 레인을 편집하지 않는다. 체크포인트나 발견한 구멍을 레인 담당에게
보고한다. GLG 는 언제든 이 분담을 override 할 수 있다.

## SSOT

| 무엇 | 어디 |
|---|---|
| 지원 상태 전체 | `applications/LEDGER.md` |
| 한 건의 정확한 기록 | `applications/<회사>--<직무>/submission.md` |
| 재사용 폼 · 면접 응답 | `applications/FAQ.md` |
| 후보 큐 | `applications/INBOX.md` |
| 이력서 **사실** | `resume/body.org` |
| 이력서 **표현** | `resume.org`, `resume/targets/*.org` |
| 탐색 거동 | `.claude/skills/linkedin-jobs/SKILL.md` |
| 다음 한 걸음 | `NEXT.md` |
| 검증된 사실 · 수치 · 계보 | `MATERIAL.md` (프로필 repo `junghan0611/junghan0611`의 `apply/` — **이 저장소가 아니다**) |

**사실은 한 벌이고, 뷰는 선택 · 배열만 한다.** 강조하고 싶다는 이유로 경력 사실을 타깃 파일에
복사하지 않는다. 지어내지 말고 `MATERIAL.md` 를 참조한다 — 실제 파일을 열어 확인한 사실만 거기 있다.
⚠ `MATERIAL.md` 는 **이 저장소에 없다.** 프로필 repo 쪽 파일이고, 이 저장소를 clone 한 사람은
갖고 있지 않다. 여기서 찾다가 「없으니 지어내도 된다」로 가지 않는다 — 없으면 GLG 에게 요청한다.

## 제출본은 두 관문을 함께 통과한다

첫 관문은 ATS·선별 에이전트가 빠르게 읽는 **명시적 적합성**이다. 직무명, 경력 기간, 필수요건과
직접 겹치는 증거, 고용·근무 자격이 첫 페이지에서 보여야 한다. 좋은 증거가 있어도 담당자가 여러
문단과 링크를 대신 추론해야 연결된다면 없는 것처럼 버려질 수 있다.

둘째 관문은 읽기 시작한 담당자에게 주는 **검증 신뢰**다. 중요한 주장은 가능하면 다음 사슬로
이어진다.

```text
JD의 요구 → 지원자의 주장 → 발생·운영 기간 → 본인 소유 범위
→ 결과·수치 → URL/명령/테스트/수용된 변경 → 이 증거가 말하지 않는 경계
```

모든 이력서 항목에 이 전부를 욱여넣지 않는다. `MATERIAL.md` (프로필 repo `junghan0611/junghan0611`의 `apply/` — **이 저장소가 아니다**)와
`resume/body.org`가 계보를 쥐고, 타깃 컷은 해당 JD가 낮은 판정 비용으로 읽을 부분만 선택한다. 문장을 매끈하게 만드는 것보다
**주장→시간→산출물→확인 입구**가 끊기지 않는 것이 우선이다.

## 불변식

### 어떤 자리에 내는가 — 회사가 아니라 **보직**을 본다 (2026-08-01 GLG)

> *「임베디드 개발자로 지원을 안 하는 건데 임베디드 회사에서도 AI 직무 연결된 것으로
> 포지셔닝은 좋아. (…) 로봇도 좋아. 헬스케어도 좋고 **AI 연결된 보직이어야 돼**.」*

**산업은 넓게 열고, 보직은 좁게 닫는다.** 임베디드 · 로봇 · 헬스케어 · 금융 — 산업은 가리지
않는다. **거르는 것은 담당업무다.**

판정은 **회사 소개가 아니라 JD 의 담당업무 항목**에서 한다. 회사 소개 문단의 「AI 기반」은
근거가 아니다 — 그 문장은 거의 모든 회사에 있다.

| | 판정 | 예 (2026-08-01 실제 판정) |
|---|---|---|
| 담당업무에 AI·에이전트·모델이 있다 | ✅ **낸다** | **Telit Cinterion** `Edge AI SW Engineer` — 담당업무에 *LLM 기반 Agent 프레임워크 도입·운영 통합*. 임베디드 회사지만 보직이 AI 축이다 |
| 회사만 AI, 담당업무는 아니다 | ⛔ **거른다** | **Bear Robotics** — AI 서비스 로봇 회사지만 보직은 로봇 리눅스·배포·인증 인프라. 담당업무 10줄에 AI 없음<br>**Sonatus** — AI-SDV 회사지만 보직은 Jenkins 온프렘 빌드·모니터링·데이터센터 운영 |
| 순수 임베디드 개발자 자리 | ⛔ **거른다** | **Telechips** `Embedded S/W Engineer` — bootloader·커널 드라이버·보드 bring-up. 기술 적합도는 높았지만 **이 축으로는 서지 않는다** |

- **적합도가 높다는 이유로 되살리지 않는다.** 텔레칩스는 자격 4줄 중 3줄이 정면이었고 공고도
  열려 있었다. 그래도 뺐다. **적합도는 이 기준을 이기지 못한다.**
- ⚠ **컷이 `_EMB` 라는 사실과 이 판정은 별개다.** Telit 은 `_EMB` 로 잡혀 있지만 JD 는 AI 축이다 —
  **컷은 조준 도구고, 이 기준은 낼지 말지다.** 둘을 섞지 않는다.

#### Override — offense mode (2026-09-09 GLG)

GLG가 명시로 위 기준을 넓혔다: **순수 임베디드 보직도 낸다. 대기업도 찔러본다.** 위 표의
판정(AI 축 없으면 거른다)은 이 시점부터 **기본값이 아니라 이전 배치의 기록**이다. 지운 것이
아니라 **현재 전략이 다르다** — 지금은 좁은 적합도보다 물량과 도달을 우선한다. `README.md`
상단 배너가 이 override의 공개 표지다.

#### 시스템 축 — FDE·AI·AX 안에서 다시 가른다 (2026-09-18 GLG)

> *「FDE와 AI/AX는 디테일을 봐야겠더라. 서비스 복제해주세요 사내 서비스 뭐 만들어줘요 이런건
> 안할거야. 내 스타일이 아니잖아. 차라리 시스템을 하는 게 맞겠어. 내 전공과 임베디드 이력에 더
> 맞아. 돈 버는 서비스 만들어줘요 이런 느낌은 나랑 안 맞거든.」*

offense mode 는 **산업과 직무명을 넓게 열어두는 결정**이고, 이 축은 그 안에서 **무엇을 만드는
자리인가**를 가른다. 둘은 충돌하지 않는다 — 임베디드는 계속 다 낸다. 다시 보는 것은
**FDE · AI Engineer · AX 라는 이름을 단 자리**다. 그 이름 하나로는 정반대의 두 일이 다 불린다.

| | 판정 | 담당업무에서 보이는 말 |
|---|---|---|
| **시스템을 만든다** | ✅ **낸다** | 런타임 · 게이트웨이 · 서빙 · 오케스트레이션 엔진 · 드라이버 · 펌웨어 · 커널 · 스케줄러 · 샌드박스 · 툴 디스패치 · 세션/정책/감사 · 권한 경계 · 관측성 · 온프레미스/폐쇄망 · 프로토콜(MCP·A2A) 구현 |
| **남의 서비스를 만들어준다** | ⛔ **거른다** | 고객사 요청을 받아 앱·기능 구현 · 사내 업무 자동화 서비스 · 「○○를 우리도 만들어 주세요」 복제 · 매출 기능 개발 · 기획한 것을 구현 |

- **판정 자리는 담당업무 항목이다.** 회사가 「플랫폼」이라고 불러도 담당업무가 고객 요청 구현이면
  서비스 쪽이고, 회사가 커머스여도 담당업무가 에이전트 런타임이면 시스템 쪽이다.
- **「고객 현장」이라는 말만으로 거르지 않는다.** FDE 의 현장성은 시스템 축과 무관하다 — 망분리
  환경에 추론 서버를 세우는 FDE 는 시스템이고, 고객 사내 업무를 앱으로 만들어 주는 FDE 는 아니다.
- **왜 이 축인가.** 전공과 임베디드 이력이 받치는 것은 시스템이지 서비스 구현 속도가 아니다.
  증거 코퍼스도 그쪽에 있다 — 서비스 축으로 서면 남과 같은 평면에서 겨루게 되고, 그건 이
  저장소가 쥔 강점이 아니다.
- ⚠ **애매한 것은 애매하다고 적는다.** 시스템·서비스가 섞인 JD 가 실제로 많다. 억지로 한쪽에
  넣지 말고 **어느 문장이 시스템이고 어느 문장이 서비스인지 인용**해 GLG 가 고르게 둔다
  (`AGENTS.md` §모른다는 것도 값이다).

**「이 저장소에 값이 없다」고 적기 전에 신원·법적 값의 SSOT 를 먼저 확인한다.**

GLG 가 실제 폼에 직접 입력한 값 — 생년월일·성별·현주소·병역(계급·병과·기간)·학력 3단(평점
포함)·직장경력(부서·직급·고용형태) — 은 한 벌로 회수돼 있다. **다만 그 원본은 이 공개
저장소가 아니라 private Org 면이 소유한다**(`PUBLICATION.md`). 완성된 지원 폼은 history 에서도
제거됐다(`docs/HISTORY-REDACTION.md` §dense-pii-form). 그러니 여기서 그 파일을 찾지 마라 —
없는 것이 정상이고, GLG 에게 private 면의 그 한 벌을 열어 달라고 요청하는 것이 맞는 경로다.

교훈 자체는 그대로 유효하다. 업스테이지 건이 2026-08-01 에 「값이 이미 있다」는 발견으로 6칸을
닫았고, **같은 날 카카오뱅크 건에서 에이전트가 같은 실수를 반복했다** — 「GLG만 채울 수 있는
5칸」이라고 적었는데 값이 이미 있었다. 케이스 파일에만 적어 두면 다음 건에서 또 놓친다.

- **모른다고 적는 것은 값이다. 그러나 「확인하지 않고 모른다고 적는 것」은 값이 아니라 오류다.**
- ⭐ **지원마다 다른 값을 내지 않는 것이 핵심이다.** 부서·직급·근무기간·병역은 회사마다 달라지면
  진다. 최종 증빙은 **공단 가입이력 증명서**이고, 스냅샷이 그것과 어긋나면 진다.
- 🔴 **값을 케이스 파일로 복사하지 않는다.** 신원·법적 값의 SSOT 는 private 면의 한 벌이다. 두 곳에
  적으면 갈라진다. 케이스에는 **어느 칸이 스냅샷의 어디로 대응하는지**만 적는다.
- ⚠ **스냅샷을 그대로 옮기면 안 되는 것 하나 — 자가 측정 수치.** 오토에버 담당업무 칸에는
  §숫자에서 걷어내기로 한 넷이 남아 있다. **정제본은 업스테이지 건**
  (`upstage--ai-engineer-agents/answers.md` §담당업무 원문)이고 그쪽을 이어 쓴다.

### 둘째 축 — 요건·증거 매트릭스 (2026-09-18, terra 교차검수)

§시스템 축은 **무엇을 만드는 자리인가**만 가른다. 그것과 **독립된 둘째 축**이 필요하다 —
시스템 축이어도 **필수요건에 GLG 의 직접 증거 입구가 없으면 낼 자리가 아니다.**

건마다 JD 의 **필수요건 3개**를 뽑아 각각에 하나를 붙인다.

| 붙이는 값 | 뜻 |
|---|---|
| **직접 공개 증거** | 열어서 확인되는 URL · 커밋 · 실행 명령이 있다 |
| **명시적 재직 사실** | 비공개 재직 수행이고, 그렇다고 표시한다 |
| **없음** | 증거가 없다. 있다고 쓰지 않는다 |

```text
직접 공개 증거 0 개 →  hold      (낼 자리가 아니다)
             1 개 →  reach     (의식적으로 2~3 건만)
             2 개 이상 → 선행 큐
```

- ⚠ **reach 를 늘리지 않는다.** 필수요건이 0 인 곳에 같은 generic 컷을 대량으로 내면
  §원칙 3 의 「common 한 사람으로 이도 저도 아닌 자리에 서지 않는다」가 **스스로 약해진다.**
  미충족을 숨기지 않으니 거짓말은 아니지만, **신호가 희석되는 것은 별개의 손해**다.
- **reach 를 고를 때는 「왜 이 간극을 감수하는가」를 한 줄로 적는다.** 못 적으면 hold 다.
- **hard gate 를 따로 본다.** production K8s · AWS 직접 설계 · 8년/10년 같은 연차 하한은
  시스템 축 판정과 무관하게 스크리닝에서 먼저 잘린다.
- 🔴 **컷이 부인하는 것을 JD 가 필수로 요구하면 내지 않는다.** 2026-09-18 위즈코어가 그
  자리였다 — `platform-engineer` 컷이 production K8s 운영을 명시적으로 부인하는데 JD 는
  그것을 필수로 요구했다. 정직한 문서 두 장이 서로 모순되는 상태로 나간다.

### Case ≠ packet ≠ ready (2026-09-18)

셋을 같은 말로 부르지 않는다. 섞으면 **진척을 실제보다 크게 보고**하게 된다.

| 단계 | 뜻 |
|---|---|
| **case** | 자리가 생겼다 — 폴더 · `JD.md` 원문 · `LEDGER.md` 행. 상태 `draft` |
| **packet** | 붙일 컷이 정해졌고 폼 문항 · 필수 슬롯 · 글자수 캡이 **실측**됐다 |
| **ready** | 사람이 제출만 하면 되는 상태 |

2026-09-18 에 22건을 만들고 「22건 제출 패킷」이라 보고했는데 **컷 미정 · 폼 미실측이었다.**
terra 교차검수가 이 구분을 되돌려 놓았다.

### 후보를 판정하기 전에 원장을 먼저 본다 (2026-09-18)

**새 후보를 「낸다」로 올리기 전에 `applications/LEDGER.md` 를 대조한다.** 회사 이름이 원장에
있으면 그 행의 상태를 읽고, 없을 때만 새 후보다.

2026-09-18 시스템 축 재분류에서 이 대조를 빼먹었다. **인핸스**가 후보로 다시 올라왔는데
원장에는 `rejected`(2026-08-04)로 이미 있었다 — GLG 판정은 **「떨어졌고 그대로 다시 지원 안
한다」**. 같은 배치에서 **모레**(2026-09-09 `rejected`)와 **스켈터랩스**·**마키나락스**(둘 다
`submitted`)도 같은 이유로 다시 올라왔다.

- **`rejected` 회사는 기본이 제외다.** 다른 직무라도 그렇다. 되살리려면 GLG 가 명시로 정한다.
- **`submitted` 회사는 「새로 낸다」가 아니라 「이미 열린 경로」다.** 마키나락스처럼 **담당자
  추천으로 들어간 건**이면 새 req 를 따로 내는 것보다 그 담당자에게 묻는 쪽이 낫다 —
  경로를 원장에서 확인하고 판단한다.
- 후보 파일에 대조 결과를 **행에 붙여 둔다**. 「원장에 없음」도 값이다.

### 제출본은 검증을 부르는 문서다 (2026-09-18 GLG)

정본은 `applications/FAQ.md` §원칙 5·6 이다. 여기서는 판정에 쓰는 형태만 적는다.

- 목표는 문서가 스스로 완결되는 것이 아니라 **읽는 쪽이 확인하러 나가게 만드는 것**이다.
  GLG 가 마키나락스 담당자에게 실제로 한 말이 기준이다 — 「에이전트랑 샅샅이 나를 검증해 주십시오」.
- **주장 옆에 확인 입구가 없으면 문장을 다듬지 말고 주장을 줄인다.**
- **애매한 수치는 아예 넣지 않는다.** 전화 면접에서 토씨 하나로 추궁당한 실측이 있다.
  §숫자 절의 허용 두 종류만 남기고, 나머지는 이력서·자소서·**면접 발화에서 모두** 뺀다.

### 인용은 열어 보고 적는다 — 그럴듯한 인용은 검증되지 않는다 (2026-09-18)

**`AGENTS.md` · `FAQ.md` 의 절을 인용할 때는 그 파일을 열어 절 이름이 실재하는지 확인한다.**
기억으로 절 이름을 쓰지 않는다. 필요한 계약이 없으면 **만들지 말고 보고**한다.

실측된 사고가 있다. `applications/hyundai-autoever--ai-agent-engineer/submission.md` 가
**`AGENTS.md §직무 선택 기준 1번`** 을 근거로 인용했는데, 그런 절은 이 저장소 history 에
**한 번도 존재한 적이 없다**(`git log --all -S'직무 선택 기준' -- AGENTS.md applications/AGENTS.md`
→ 0건. ⚠ 경로를 빼면 그 인용을 처음 적은 케이스 파일이 잡힌다 — 절의 증거가 아니라 오염의
출처다). 2026-07-28 경에 들어가
그대로 푸시됐고 넉 달 동안 아무도 열어보지 않았다.

그리고 **2026-09-18 에 그 거짓이 재생산됐다.** 새 케이스 22건을 만들던 에이전트가 기존
`submission.md` 들을 패턴으로 읽다가 같은 인용을 `beauty-selection` 건에 그대로 옮겼다.
교차검수에서 잡혀 둘 다 정정했다.

- **이 저장소의 값은 판정 계보다.** 계보에 실재하지 않는 조항이 박히면 계보 전체의 신뢰가 깎인다.
  공개 저장소라 더 그렇다.
- ⚠ **기존 케이스 파일을 「패턴」으로 읽을 때 조심한다.** 형식은 배우되 **인용은 물려받지
  않는다.** 앞 사람이 적었다는 것은 그것이 사실이라는 근거가 아니다.
- 판단 자체가 맞고 출처만 거짓이면 **판단은 남기고 출처를 뗀다.** 판정 계보를 지우지 않는다.

### 진실과 증거

- 검증된 사실 코퍼스가 받쳐주지 않는 **모델 학습 · 파인튜닝 · 프레임워크 · 프로토콜 구현**을
  주장하지 않는다.
- **자기 능력(self-owned) · 수용한 제3자 기술 · 비공개 재직 수행 사실 · 공개 인접 구현**을 구분한다.
  넷을 같은 증거처럼 섞지 않는다.
- "검증 가능"은 눈에 보이는 URL · 실행 명령 · 테스트/게이트 · 수용된 변경 · **명시적으로 재직
  사실이라고 표시한 주장** 중 하나로 이어져야 한다.
- **모른다는 것도 값이다.** 미확인 근무지 · 요건을 보이는 채로 둔다. 추론해서 채우거나 조용히
  버리지 않는다.

### 숫자 — 내가 만든 숫자는 빼고, 시간축을 적는다 (2026-07-29 GLG)

**성능·작업량 수치는 넣어봐야 도움이 안 된다.** 그 자리에는 **그 일을 한 커밋과 타임라인
기록**을 적는다. 시간축에서 훨씬 낫고, 그것이 이 저장소가 가진 실제 강점이다.

가르는 기준은 「크냐 작냐」가 아니라 **「내가 만든 숫자냐, 남이 남긴 기록이냐」**다.

⛔ **빼는 것 — 자가 측정 수치**

- **성능 수치**: 부팅 45.4→5.8초, 메모리 816→246MiB 류. 벤치마크 자랑이고 외부 검증이 없다.
  이 건은 더 나쁘다 — **개선한 것은 업스트림 릴리스**였고, 우리 몫은 진단·롤백 기준·정책이다.
  숫자를 적으면 남의 성과가 내 성과처럼 읽히고 면접에서 「그래서 본인이 뭘 하셨죠」가 나온다.
- **자가 측정 작업량 수치**: 테스트 163개, 코드 공유 96%, 임베딩 2,945건, 하루 24커밋 류.
  스스로 세어 스스로 적은 숫자라 검증 축이 없고, 규모로 읽히는 자리에서는 **오히려 작아 보인다.**
- 2026-07-29 에 `resume/body.org` · `resume/targets/` 에서 전부 걷어내고 여섯 컷을 재빌드했다.

✅ **남기는 것 — 대체 불가능한 기록**

- **라이브 주소에서 확인되는 누적 기록**: Working Corpus(노트 · 서지 · 커밋 · 연속 저널일 ·
  가든 페이지 · 건강 기록일 · 스킬 수) + `agenda.junghanacs.com` + 측정일.
  읽는 쪽이 **클릭해서 확인한다.** 오래 쌓아야만 나오는 수라 대체 불가능하다.
- **제3자가 남긴 숫자**: 기여받은 PR 의 파일·줄 수, npm 설치 수. **남의 행동이 남긴 기록**이라
  성격이 다르다.
- **사건의 시간축**: 날짜 · 시각 · 커밋 SHA · PR 번호. 공개 커밋은 클릭 가능한 링크로 단다.
  **숫자를 뺀 자리는 이것으로 대신한다.**

세부 규칙은 `applications/FAQ.md` §원칙 4 와 `dossier/PORTFOLIO-PLAN.md` §수치 원칙 v4.

### 정보가 부족할 때 남기는 요청

`추가 정보 필요`라고만 쓰지 않는다. 다음 네 가지를 해당 SSOT에 남긴다.

1. **무엇을 확인했는가** — 읽은 URL·파일·명령.
2. **정확히 무엇이 비어 있는가** — 원문, 폼 문항, 날짜, 사람 결정값 등.
3. **왜 다음 상태로 못 가는가** — 적합도 판정, 문서 생성, `ready`, 제출 중 무엇을 막는지.
4. **GLG가 줄 최소 입력은 무엇인가** — 본문 복사, 스크린샷, 값 하나, 선택 하나처럼 답할 수 있게.

증거 코퍼스에 없는 경력은 “다른 증거가 없다면 미충족으로 표시한다”고 요청한다. 로그인 뒤에만
보이는 폼은 공개 경로에서 확인한 범위와 브라우저에서 확인할 항목을 나눈다. 질문 자체를 Git에
남길 수는 있지만 민감한 답값은 남기지 않는다.


### 사람의 권한

- 에이전트는 폼을 준비할 수 있지만 **최종 Submit 은 건별 승인 없이 누르지 않는다.**
- 개인 · 법적 값, 동의, 연봉, 되돌릴 수 없는 계정 변경은 사람 소관이다. 민감값은 **Git 에 저장하지
  않고 브라우저에서만** 입력할 수 있다.
- **한 회사 한 직무는 기본 전략이지 법이 아니다.** GLG 가 명시로 예외를 택하면 그대로 가고, 앞선
  권고를 없던 일로 고쳐 쓰지 말고 **override 와 이유를 기록**한다.

### 기록 닫힘

한 건은 셋이 모두 일치할 때만 닫힌다.

1. 사람이 최종 행위 완료를 보고했고,
2. `applications/LEDGER.md` 가 날짜와 함께 `submitted` 이고,
3. 해당 `submission.md` 가 `submitted` 이며 업로드 스냅샷을 지목하고, 개인 필드는 사람이 완료했으며
   값은 저장하지 않았다고 적혀 있다.

사람의 보고 **직후** 갱신한다. 타깃 기록이 아직 `ready` 인데 요약이 "제출 완료"라고 말하면 안 된다.
검사: `applications/check.py`.

### 산출물 안전

- **소스에서 빌드한다.** PDF 를 손으로 고치지 않는다.
- 업로드 직전 **보이는 파일명을 확인한다.** ATS · LinkedIn 에 저장된 기본 파일이 다른 타깃용
  문서를 가리키고 있을 수 있다.
- 업로드한 **그 파일은 private 제출 보관면에 둔다.** public `submit/`에는 파일명·source·지문만
  남긴다. 런처는 계속 바뀌지만 실제 바이너리를 공개 Git으로 복제하지 않는다.
- 자격증명 · 개인 연락처 · 법적 값 · 원시 고객 식별자 · 개인화 ATS URL · 세션 JSONL · 로컬
  timeline 스냅샷 · 제3자 원문은 커밋하지 않는다. 회사명과 공개 가능한 지원 답변은 남긴다.

## 품질 게이트

```bash
# 탐색 로직
.claude/skills/linkedin-jobs/test_ljobs.py
python3 -m py_compile \
  .claude/skills/linkedin-jobs/ljobs.py \
  .claude/skills/linkedin-jobs/test_ljobs.py

# 지원 기록 닫힘
applications/check.py

# 이력서 빌드와 PDF 검사
(cd resume && ./run.sh all && ./run.sh verify)

# dossier 발사대 — dossier/ 를 건드릴 때만
(cd dossier && ./run.sh check && ./run.sh all && ./run.sh verify)

# 공고가 아직 열려 있는가 (제출 직전)
applications/alive.py
```

게이트와 문서가 어긋나면 **같은 변경 안에서** 거동이나 SSOT 중 하나를 고친다.

## 공개는 현재 계약이다

이 저장소는 **실패와 판단 계보를 공개하되 공개할 권리가 없는 값은 분리하는 작업면**이다.
`PUBLICATION.md`의 history 정제와 검증을 통과한 전체 commit 흐름이 공개 기록이며, 한 커밋짜리
allowlist export가 아니다. 이후 모든 편집은 같은 경계를 지킨다.

- 방법·계약·결과는 공개한다.
- raw 폼·법적 값·개인 연락처·보상값·추천서 원문·받은 과제·제출 바이너리는 private 면에 둔다.
- 공고 전문을 복제하지 않고 URL·필요 인용·요약으로 남긴다.
- 공개 후 삭제는 회수가 아니다. 애매한 값은 commit 전에 멈춘다.

## 협업 보고

체크포인트는 상태 변화나 막힘으로 연다.

- 무엇이 바뀌었는가;
- 무엇이 아직 모르는 채인가;
- 다음 편집을 어느 SSOT 가 쥐는가;
- 검증 결과가 정확히 무엇인가;
- GLG 가 결정하거나 손대야 할 것이 있는가.

교차 검수는 다른 에이전트에 대한 판결이 아니다. 구멍이 나왔다는 건 레인 분담이 작동했다는 뜻이다.
