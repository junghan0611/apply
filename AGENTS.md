# AGENTS.md — apply (비공개 채용 지원 작업 저장소)

`apply` 는 **증거 우선 · 사람이 최종 결정하는** 채용 지원 워크스페이스다. 탐색 · 문서 생성 ·
브라우저 제출 · 메타 검수를 레인으로 갈라, 병렬로 도는 에이전트가 서로의 SSOT 를 덮지 않게 한다.

GitHub `junghan0611/apply` **(PRIVATE)**. 회사명 · 고객사 · 사내 사실 · 추천 경로 · 지원 답변을
**그대로 담는다.** 여기서 정보를 가리기 시작하면 작업이 안 된다. 공개는 별도 트랙이고 나중 일이다
(§공개는 나중에).

## 여기부터 읽는다

1. `NEXT.md` — 지금 할 한 걸음.
2. 손댈 레인의 `AGENTS.md` — 그 레인의 계약.
3. `git status --short` — 옆 파일을 다른 에이전트가 쥐고 있을 수 있다.
4. `edit`/`write` 전에 기존 파일을 읽는다. 기억으로 파일을 덮는 것이 동시 작업을 날리는 경로다.
5. **커밋 · 푸시는 GLG 가 이번 세션에서 명시로 요청할 때만 한다.**

## 레인과 소유권

| 레인 | 위치 | 소유 | 소유하지 않음 |
|---|---|---|---|
| 탐색 | `.claude/skills/linkedin-jobs/` | 공개 카드 · 포스트, 원문 요건, 근무지 확신도, 랭킹 근거, 추천 컷 | 로그인, 개인 필드, 제출 |
| 문서 | `resume/` | 검증된 사실 모듈, 직무별 선택 · 순서, 공개 증거 입구, 재현 가능한 PDF | ATS 상태, 지원 원장 |
| 제출 | `applications/` | JD 보존, 타깃별 답변, 업로드 파일 사본, 상태 원장 | 공용 이력서 사실, 탐색 구현 |
| 닫힌 dossier | `nhn/` | 제출본 재생성과 면접 앵커 | 현재 지원 큐 |
| 메타 검수 | 루트 문서 · `scripts/` | 계약, 모순 검사, 핸드오프 | 남의 레인 구현을 말없이 다시 여는 것 |

한 에이전트가 "도와준다"며 다른 레인을 편집하지 않는다. 체크포인트나 발견한 구멍을 레인 담당에게
보고한다. GLG 는 언제든 이 분담을 override 할 수 있다.

## SSOT

| 무엇 | 어디 |
|---|---|
| 지원 상태 전체 | `applications/LEDGER.md` |
| 한 건의 정확한 기록 | `applications/<회사>--<직무>/submission.md` |
| 재사용 폼 · 면접 응답 | `applications/FAQ.md` |
| 후보 큐 | `applications/INBOX.md` |
| 이력서 **사실** | `resume/body.org` |
| 이력서 **표현** | `resume.org`, `resume/targets/*.org` |
| 탐색 거동 | `.claude/skills/linkedin-jobs/SKILL.md` |
| 다음 한 걸음 | `NEXT.md` |
| 검증된 사실 · 수치 · 계보 | `~/repos/gh/junghan0611/apply/MATERIAL.md` |

**사실은 한 벌이고, 뷰는 선택 · 배열만 한다.** 강조하고 싶다는 이유로 경력 사실을 타깃 파일에
복사하지 않는다. 지어내지 말고 `MATERIAL.md` 를 참조한다 — 실제 파일을 열어 확인한 사실만 거기 있다.

## 불변식

### 진실과 증거

- 검증된 사실 코퍼스가 받쳐주지 않는 **모델 학습 · 파인튜닝 · 프레임워크 · 프로토콜 구현**을
  주장하지 않는다.
- **자기 능력(self-owned) · 수용한 제3자 기술 · 비공개 재직 수행 사실 · 공개 인접 구현**을 구분한다.
  넷을 같은 증거처럼 섞지 않는다.
- "검증 가능"은 눈에 보이는 URL · 실행 명령 · 테스트/게이트 · 수용된 변경 · **명시적으로 재직
  사실이라고 표시한 주장** 중 하나로 이어져야 한다.
- **모른다는 것도 값이다.** 미확인 근무지 · 요건을 보이는 채로 둔다. 추론해서 채우거나 조용히
  버리지 않는다.

### 사람의 권한

- 에이전트는 폼을 준비할 수 있지만 **최종 Submit 은 건별 승인 없이 누르지 않는다.**
- 개인 · 법적 값, 동의, 연봉, 되돌릴 수 없는 계정 변경은 사람 소관이다. 민감값은 **Git 에 저장하지
  않고 브라우저에서만** 입력할 수 있다.
- **한 회사 한 직무는 기본 전략이지 법이 아니다.** GLG 가 명시로 예외를 택하면 그대로 가고, 앞선
  권고를 없던 일로 고쳐 쓰지 말고 **override 와 이유를 기록**한다.

### 기록 닫힘

한 건은 셋이 모두 일치할 때만 닫힌다.

1. 사람이 최종 행위 완료를 보고했고,
2. `applications/LEDGER.md` 가 날짜와 함께 `submitted` 이고,
3. 해당 `submission.md` 가 `submitted` 이며 업로드 스냅샷을 지목하고, 개인 필드는 사람이 완료했으며
   값은 저장하지 않았다고 적혀 있다.

사람의 보고 **직후** 갱신한다. 타깃 기록이 아직 `ready` 인데 요약이 "제출 완료"라고 말하면 안 된다.
검사: `applications/check.py`.

### 산출물 안전

- **소스에서 빌드한다.** PDF 를 손으로 고치지 않는다.
- 업로드 직전 **보이는 파일명을 확인한다.** ATS · LinkedIn 에 저장된 기본 파일이 다른 타깃용
  문서를 가리키고 있을 수 있다.
- 업로드한 **그 파일을** 그 건의 `submit/` 에 복사한다. 런처는 계속 바뀐다.
- 자격증명 · 원시 고객 식별자 · 세션 JSONL · 로컬 timeline 스냅샷은 커밋하지 않는다.
  (개인 연락처와 회사명은 이 저장소에 담아도 된다 — PRIVATE 이다.)

## 품질 게이트

```bash
# 탐색 로직
.claude/skills/linkedin-jobs/test_ljobs.py
python3 -m py_compile \
  .claude/skills/linkedin-jobs/ljobs.py \
  .claude/skills/linkedin-jobs/test_ljobs.py

# 지원 기록 닫힘
applications/check.py

# 이력서 빌드와 PDF 검사
(cd resume && ./run.sh all && ./run.sh verify)

# 닫힌 dossier — nhn/ 을 건드릴 때만
(cd nhn && ./run.sh check && ./run.sh all && ./run.sh verify)
```

게이트와 문서가 어긋나면 **같은 변경 안에서** 거동이나 SSOT 중 하나를 고친다.

## 공개는 나중에

지금 이 저장소는 **전부 담는 비공개 작업면**이다. 회사명 · 답변 · 제출본을 가리지 않는다.

언젠가 "기록을 이렇게 했었다"를 공개할 때가 오면 그때 `PUBLICATION.md` 를 읽는다. 요지만:
현재 Git history 를 그대로 public 으로 바꾸지 않고, allowlist 로 **새 트리 + 새 history** 를 만든다.
그때까지 이 문서 어디에도 공개 검열을 끌어들이지 않는다.

## 협업 보고

체크포인트는 상태 변화나 막힘으로 연다.

- 무엇이 바뀌었는가;
- 무엇이 아직 모르는 채인가;
- 다음 편집을 어느 SSOT 가 쥐는가;
- 검증 결과가 정확히 무엇인가;
- GLG 가 결정하거나 손대야 할 것이 있는가.

교차 검수는 다른 에이전트에 대한 판결이 아니다. 구멍이 나왔다는 건 레인 분담이 작동했다는 뜻이다.
