# AGENTS.md — junghan0611/apply (비공개 채용 지원 작업 저장소)

이 저장소는 공개 프로필이 아니라 **실제 채용 공고에 제출할 서류를 만드는 비공개 작업
저장소**다. GitHub `junghan0611/apply` (PRIVATE). 회사명·고객사·사내 사실·추천 경로를
담을 수 있으며, 공개 웹 트랙과는 분리해서 다룬다.

멀티 타깃 워크스페이스다. 채용 공고 한 건마다 하위 디렉터리를 하나씩 둔다. 지금
활성 타깃은 **NHN 하나뿐이고**, 담당 디렉터리는 `nhn/` 이다.

---

## 담당자(steward)를 세운다

이 저장소에서 일하는 담당자는 **한 번에 활성 공고 한 건에만 완전히 집중한다.** 지금은
NHN이다. 이력서 일반론을 쓰는 사람이 아니라, **아래 「NHN 공고」에 정확히 대응하는
제출본 3종을 완성으로 끌어올리는 사람**이다.

### 정직한 현재 진단 (2026-07-16, GLG)

- `nhn/` 의 Org 문서 3종은 **초안 수준이고 아직 많이 부족하다.**
- 이미지 6장은 **껍데기 배너 그림**이다. 사실 구조를 보여주는 인포그래픽이 아니다.
- **이 완성도로 제출하는 것은 가드너로서 있을 수 없는 일이다.** 파이프라인이 도는 것과
  낼 수 있는 것은 다르다. 담당자의 일은 "빌드가 된다"가 아니라 "이 사람이 이걸 냈다"가
  성립하는 수준까지 내용을 올리는 것이다.

### 담당자의 일

1. **NHN 공고 하나에 조준한다.** 아래 공고 명세를 서류의 채점 기준으로 삼는다. 공고에
   없는 자랑은 상세 MD로 내리거나 뺀다.
2. **내용을 먼저 올린다.** 프로젝트별 문제·역할·판단·수치·증거가 실제로 검증 가능한
   수준까지. 소재는 지어내지 않고 `junghan0611/apply/MATERIAL.md`(아래 §참조)에서 가져온다.
3. **이미지를 진짜 인포그래픽으로 만든다.** 배너가 아니라 사실 구조·경계·변화를 그린다.
   실패 시 폴백은 결정적 다이어그램(graphviz/mermaid).
4. **빌드·검증·패키지는 `nhn/AGENTS.md` 계약을 따른다.** 파생물(ODT/PDF/MD)을 손으로
   고치지 않는다. Org·이미지·파이프라인을 고치고 재빌드한다.
5. **막힘·구조 변경·첫 판·강조 순서는 GLG(및 지정 PM)에게 체크포인트로 보고한다.**
   최종 강조 순서와 커밋·푸시는 GLG가 결정한다.

---

## NHN 공고 — 정확히 (제출 채점 기준)

출처: `NHN.md`. 담당자는 이 명세를 서류의 목표 함수로 쓴다.

**공고 정체.** 제목은 "AX 전환 / AI 인프라(백엔드)"지만, 주요 업무의 절반이 문서·교육이다.
즉 **빌드(에이전트 플랫폼 구축·운영) + 전파·인에이블(문서·배포·교육)의 이중 직무**다.
여기서 말하는 "빌드"는 "AI 기능이 붙은 백엔드"가 아니라 **"에이전트 플랫폼을 세우고,
관리하고, 운영한다"**이다 (2026-07-13 GLG 축 교정, MATERIAL.md §5).

### 주요 업무

- AX 전환을 위한 시스템 인프라 구축·관리 및 서비스 설계·개발·운영
- 그룹사 AI 관련 문서(가이드·위키 등) 작성·배포 및 교육 활동

### 자격 요건

- Linux 환경 개발·운영 경험
- Claude Code·Codex 등 Agentic AI 활용 개발 경험
- Java·Python·Go 등 1개 이상 언어로 BE 시스템 개발·운영 경험
- 개발·비개발 영역 AX 전환에 대한 관심

### 우대 사항

- Web Framework(Spring·Gin·FastAPI) 이해
- DB·MQ·VM·컨테이너 등 시스템 이해
- LLM·RAG 등 AI 기술·이론 이해
- BMAD 같은 AI Workflow 이해
- 클라우드 환경 서비스 개발 경험
- FE(React·Tailwind CSS) 이해·개발 경험
- 조직·구성원 협업·커뮤니케이션
- 개발자·비개발자 대상 문서 작성

### 제출물 (포트폴리오 제출란에 업로드)

1. `KimJunghan_AX_Competency.pdf` — JD 관련 역량·성과 기술서
2. `KimJunghan_AX_Portfolio.pdf` — 이미지·프로젝트 중심 포트폴리오
3. `KimJunghan_AX_Detail.md` (+ 증거 ZIP) — 역량·성과·포트폴리오 상세 MD

### 전형 절차 (서류 설계에 반영)

서류 → 전화 인터뷰 → **T인터뷰(Tech, 라이브 코딩 — Claude Code 활용)** → C인터뷰(Career)
→ 처우 협의 → 입사. 근무지: 판교 삼평동 플레이뮤지엄.

- **T인터뷰가 라이브 코딩이라는 점이 서류 설계를 좌우한다.** 상세 MD의 「Tech Interview
  anchors」는 그 자리에서 열어 설명할 파일·테스트·설계 판단의 진입점이다. 서류가 라이브
  코딩에서 재현·시연될 수 있게 만든다 (예: `make`로 제출물 재생성 시연).

### 추천서가 짚은 4개 축 (서류가 증거를 대야 함)

추천서는 주장을 하고 서류는 증거를 댄다. **추천서를 서류 본문에 인용하지 않는다** — 추천은
추천인의 문장, 서류는 내 문장. 두 개가 같은 사실을 다른 자리에서 가리키게 둔다.

| 추천서의 주장 | 서류가 증거를 대는 곳 (현행 `nhn/`) |
|---|---|
| 펌웨어→서버→앱까지 끝을 본다 | `portfolio.org` 프로젝트 5 (Linux·Backend·제품 배경) |
| 도구를 갖다 쓰는 게 아니라 위임·검증 방식을 만든다 | `portfolio.org` 프로젝트 4 (entwurf·forge-config) |
| RAG를 데모가 아니라 운영으로 돌렸다 | `competency.org` 서빙 계층, `portfolio.org` 프로젝트 1·3 |
| 알게 된 걸 남이 이어받게 정리한다 | `competency.org` 문서 3층, `portfolio.org` 전파·온보딩 |

---

## 참조 — junghan0611 리포 (소재 창고 + 공개 트랙)

담당자는 이 저장소에서 일하지만, **소재와 사실은 `~/repos/gh/junghan0611`** 에서 가져온다.
junghan0611은 공개 디지털 가든 허브이자 공개 AX 도시에다. 지어내지 말고 여기를 참조한다.

| 경로 | 무엇인가 | 담당자의 사용법 |
|---|---|---|
| `junghan0611/apply/MATERIAL.md` | **검증된 사실 창고 + GLG 축 교정.** 실제 파일을 열어 확인한 사실만. 중심축(openclaw 운영→andenken→entwurf→forge-config)이 여기 있다 | **정본 소재 SSOT.** 수치·계보·프레이밍은 여기서. 초안이 이미 틀린 것(§0)도 여기 정리됨 |
| `junghan0611/apply/ax/` | **공개 게이트 AX 도시에** → `ax.junghanacs.com`. LaTeX/XeLaTeX 5뷰(landing/record/competency/portfolio/detail)를 `ax.org` 한 장에서 생성 | 같은 사람의 **공개(필터된) 표면**. 구조·순서의 참고본. 단 공개본은 회사명·경로를 게이트한다 |
| `junghan0611/apply/ax/ax.org` | 공개 5뷰의 **단일 저작 소스** | 공개 서사의 골격 참고 |
| `junghan0611/AGENTS.md` | 리포 정체·가든 데이터·문서 규율 | GLG(=김정한=힣) 정체와 가든 규율의 기준선 |
| `notes.junghanacs.com` · `agenda.junghanacs.com` | 공개 가든 · 라이브 통계(`/api/stats`) | 노트 수·커밋 수 등 라이브 수치의 재현 가능한 근거 |

### 공개(junghan0611/ax) ↔ 비공개(apply/nhn) 관계

- **같은 사람, 다른 표면.** 공개 도시에는 회사명·추천인·지원 경로·근무지를 게이트하고
  `leakwords.txt` + `make check`로 누출을 막는다. **비공개 제출본(nhn)은 회사명·사내 사실을
  쓸 수 있다** — gitignore된 채 채용사에 직접 업로드되기 때문.
- **자동 동기화하지 않는다.** 비공개 소재가 공개 트랙으로 새지 않게 한다. 공개본은 제출 후
  별도 필터링 트랙으로만 반영한다.
- **회사 실명(헤이홈/고퀄) 노출 수위는 GLG 최종 판단.** 현행 제출본은 gitignore 전제로
  "헤이홈"을 노출 중. 제출 직전 GLG가 수위를 확정한다.

---

## 두 레인 분담 (2026-07-16 GLG 결정)

같은 소재(MATERIAL.md)를 쓰되 산출 표면이 다르므로 두 레인으로 나눠 병행한다.

| 레인 | 담당 | 작업 위치 | 목표 |
|---|---|---|---|
| **제출 레인** | apply/nhn 담당자 | `apply/nhn/` (이 리포) | NHN 제출본 3종 완성 |
| **공개 레인** | junghan0611 세션 | `junghan0611/apply/ax/` | `ax.junghanacs.com` 업그레이드 |

- 두 레인은 **MATERIAL.md를 공유 사실원**으로 쓴다. 서로의 파생물을 손대지 않는다.
- 제출 레인이 우선순위다. 오늘의 목표는 **NHN 제출**이다.

---

## 경계와 금지

- **제출 문서 본문에서 제외:** 추천인 실명, 지원 경로(사내 추천·부서·이름), 근무지.
  이것들은 이 저장소 메모(`NHN.md`)에는 있어도 제출 PDF/MD 본문에는 넣지 않는다.
- **절대 금지:** 원시 고객 식별자(uid/devId/phone/email), 자격증명, 식별 가능한 운영 데이터.
- **비공개→공개 자동 동기화 금지.** 공개 트랙은 별도 필터링으로만.
- **능력(self-owned)·수용(third-party)·비공개 수행 사실·경계(not used)를 혼동하지 않는다.**
  직접 안 쓴 프레임워크를 썼다고 쓰지 않는다. 경계를 먼저 밝히고 인접 능력만 매핑한다.
- **패키지는 allowlist 방식.** `.git`·캐시·임시 빌드·편집기 잠금·로컬 절대경로·원본
  `PRIVATE.md`를 넣지 않는다.

---

## 파이프라인·검증 (SSOT는 nhn/AGENTS.md)

`nhn/` 의 빌드·검증·패키지 계약은 **`nhn/AGENTS.md`가 SSOT**다. 요지만:

```bash
cd nhn
./run.sh check       # 도구·폰트 확인
./run.sh all         # competency + portfolio + detail 빌드
./run.sh verify      # 페이지 수·판형·한글 텍스트·캡션 연번·:noexport 미누출·연락처
./run.sh package     # allowlist ZIP 조립 + 절대경로 누출 검사
```

검증 통과 조건: placeholder 문자열 0건, 글자 깨짐·사실 왜곡 0건, `:noexport` 프롬프트·검토
메모 미누출, ZIP `unzip -t` 통과, 상대경로·재빌드 명령 확인.

---

## 협업

- 구현 담당은 구조 변경·첫 PDF·이미지 프롬프트·빌드 결과·막힘을 지정 PM에게 체크포인트로
  보고한다.
- **최종 강조 순서·커밋·푸시는 GLG가 결정한다.** 커밋 요청이 곧 푸시 요청은 아니다.
- 이 저장소는 PRIVATE지만, 전역 커밋/푸시 안전 훅(identity·secret 스캔)은 그대로 적용된다.
